"""
Options-flow gate pack extracted from _assemble_signal().

Gates (applied in pipeline order):
  OptionsFlowConfirmationGate  — PC ratio hard-block; call sweep + GEX bonus/penalty
  IvRankFlagGate               — informational elevated-IV disclosure (no score change)
  IvrMrGate                    — §48 IVR MR amplifier (score ±3–5 for dealer unwind)
  PutCallSkewGate              — §49 25d put skew panic-precision signal (score +4)
  IvTermStructureGate          — IV term spike MR amplifier (score +4)
  PutSweepCapitulationGate     — extreme put sweep capitulation confirmation (+5pp)

All gates guard on ctx.action == "BUY" and ctx.has_mr where applicable;
a prior gate setting action = "HOLD" causes them to skip naturally.

Unit test pattern:
    from services.gates.base import SignalContext
    from services.gates.options import IvrMrGate

    ctx = SignalContext(
        action="BUY", confidence=60.0, score=50.0, rationale=[], sources=set(),
        has_mr=True, vix=22.0, opt_flow={"iv_rank": 65},
        ...
    )
    IvrMrGate().apply(ctx)
    assert ctx.score == 55.0
"""

from __future__ import annotations

from .base import GateBase, SignalContext


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut: MutantDict = {}  # type: ignore


class OptionsFlowConfirmationGate(GateBase):
    """
    Options flow / GEX confirmation gate.

    Hard block: extreme put dominance (P/C > 2.0) → HOLD.
    Bonus (call sweep + positive GEX):     +15pp — highest-conviction MR setup.
    Bonus (positive GEX + call-dominant):  +5pp.
    Penalty (negative GEX + puts + low UV): −5pp.

    Only applies to MR BUY entries with opt_flow present.
    """

    @_mutmut_mutated(mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut)
    def apply(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_orig(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_1(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr and not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_2(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" and not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_3(self, ctx: SignalContext) -> None:
        if ctx.action == "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_4(self, ctx: SignalContext) -> None:
        if ctx.action != "XXBUYXX" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_5(self, ctx: SignalContext) -> None:
        if ctx.action != "buy" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_6(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_7(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_8(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = None
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_9(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get(None)
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_10(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("XXpc_ratioXX")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_11(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("PC_RATIO")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_12(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = None
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_13(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(None)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_14(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") and 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_15(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get(None) or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_16(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("XXgexXX") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_17(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("GEX") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_18(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 1.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_19(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = None
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_20(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get(None)
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_21(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("XXunusual_vol_ratioXX")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_22(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("UNUSUAL_VOL_RATIO")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_23(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = None
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_24(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(None):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_25(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_26(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "XXn/aXX"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_27(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "N/A"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_28(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = None

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_29(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(None)

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_30(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get(None))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_31(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("XXsweep_callsXX"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_32(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("SWEEP_CALLS"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_33(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None or float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_34(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_35(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(None) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_36(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) >= 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_37(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 3.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_38(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = None
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_39(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "XXHOLDXX"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_40(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "hold"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_41(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add(None)
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_42(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("XXRisk GateXX")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_43(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("risk gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_44(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("RISK GATE")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_45(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                None
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_46(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "XXsrcXX": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_47(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "SRC": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_48(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "XXRisk GateXX",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_49(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "risk gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_50(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "RISK GATE",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_51(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "XXheadXX": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_52(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "HEAD": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_53(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(None):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_54(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "XXbodyXX": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_55(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "BODY": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_56(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(None):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_57(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "XXinstitutional hedging at scale, not retail panic. When professionals are XX"
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_58(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. when professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_59(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "INSTITUTIONAL HEDGING AT SCALE, NOT RETAIL PANIC. WHEN PROFESSIONALS ARE "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_60(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "XXbuying puts this aggressively on an oversold stock, they expect further XX"
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_61(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "BUYING PUTS THIS AGGRESSIVELY ON AN OVERSOLD STOCK, THEY EXPECT FURTHER "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_62(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "XXdownside — not a bounce. MR setups require fear exhaustion; extreme put XX"
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_63(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. mr setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_64(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "DOWNSIDE — NOT A BOUNCE. MR SETUPS REQUIRE FEAR EXHAUSTION; EXTREME PUT "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_65(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "XXflow confirms the fear is still building.XX"
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_66(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "FLOW CONFIRMS THE FEAR IS STILL BUILDING."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_67(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "XXsentimentXX": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_68(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "SENTIMENT": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_69(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "XXnegXX",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_70(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "NEG",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_71(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "XXmetaXX": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_72(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "META": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_73(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(None):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_74(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls or gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_75(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex >= 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_76(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 1:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_77(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = None
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_78(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(None, 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_79(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), None)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_80(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_81(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), )
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_82(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(None, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_83(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, None), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_84(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_85(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_86(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(96.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_87(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence - 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_88(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 16), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_89(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 2)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_90(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add(None)
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_91(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("XXOptionsXX")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_92(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_93(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("OPTIONS")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_94(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = None
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_95(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex * 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_96(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1000001.0:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_97(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(None) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_98(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) > 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_99(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1000001.0 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_100(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                None
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_101(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "XXsrcXX": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_102(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "SRC": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_103(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "XXOptionsXX",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_104(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_105(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "OPTIONS",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_106(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "XXheadXX": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_107(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "HEAD": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_108(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "XXCall Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)XX",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_109(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "call sweep + positive gex — highest-conviction mr setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_110(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "CALL SWEEP + POSITIVE GEX — HIGHEST-CONVICTION MR SETUP (+15PP)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_111(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "XXbodyXX": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_112(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "BODY": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_113(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "XXaccumulating aggressively, not passively. Positive GEX means dealers must XX"
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_114(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. positive gex means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_115(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "ACCUMULATING AGGRESSIVELY, NOT PASSIVELY. POSITIVE GEX MEANS DEALERS MUST "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_116(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "XXmechanically buy the dip to stay hedged, creating a structural support floor. XX"
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_117(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "MECHANICALLY BUY THE DIP TO STAY HEDGED, CREATING A STRUCTURAL SUPPORT FLOOR. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_118(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "XXThese two forces reinforce the MR bounce thesis.XX"
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_119(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "these two forces reinforce the mr bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_120(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "THESE TWO FORCES REINFORCE THE MR BOUNCE THESIS."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_121(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "XXsentimentXX": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_122(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "SENTIMENT": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_123(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "XXposXX",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_124(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "POS",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_125(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "XXmetaXX": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_126(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "META": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_127(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 or float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_128(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None or gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_129(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_130(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex >= 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_131(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 1 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_132(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(None) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_133(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) <= 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_134(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 1.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_135(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = None
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_136(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(None, 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_137(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), None)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_138(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_139(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), )
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_140(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(None, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_141(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, None), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_142(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_143(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_144(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(96.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_145(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence - 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_146(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 6), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_147(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 2)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_148(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add(None)
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_149(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("XXOptionsXX")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_150(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_151(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("OPTIONS")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_152(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                None
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_153(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "XXsrcXX": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_154(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "SRC": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_155(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "XXOptionsXX",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_156(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_157(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "OPTIONS",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_158(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "XXheadXX": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_159(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "HEAD": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_160(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(None):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_161(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "XXbodyXX": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_162(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "BODY": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_163(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex * 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_164(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1000001.0:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_165(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(None):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_166(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "XXit dips — creating a mechanical support floor. Call dominance confirms XX"
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_167(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_168(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "IT DIPS — CREATING A MECHANICAL SUPPORT FLOOR. CALL DOMINANCE CONFIRMS "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_169(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "XXinstitutional accumulation, not distribution.XX"
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_170(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "INSTITUTIONAL ACCUMULATION, NOT DISTRIBUTION."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_171(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "XXsentimentXX": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_172(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "SENTIMENT": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_173(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "XXposXX",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_174(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "POS",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_175(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "XXmetaXX": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_176(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "META": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_177(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(None):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_178(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 or (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_179(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 or float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_180(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None or gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_181(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_182(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex <= 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_183(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 1 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_184(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(None) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_185(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) >= 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_186(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 2.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_187(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None and float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_188(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is not None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_189(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(None) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_190(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) <= 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_191(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 1.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_192(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = None
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_193(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(None, 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_194(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), None)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_195(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_196(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), )
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_197(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(None, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_198(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, None), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_199(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_200(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_201(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(36.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_202(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence + 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_203(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 6), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_204(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 2)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_205(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add(None)
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_206(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("XXRisk GateXX")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_207(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("risk gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_208(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("RISK GATE")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_209(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                None
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_210(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "XXsrcXX": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_211(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "SRC": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_212(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "XXRisk GateXX",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_213(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "risk gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_214(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "RISK GATE",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_215(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "XXheadXX": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_216(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "HEAD": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_217(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(None):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_218(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "XXbodyXX": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_219(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "BODY": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_220(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) * 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_221(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(None) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_222(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1000001.0:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_223(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(None):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_224(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "XXDealers short gamma will amplify moves in both directions — volatility unpinned. XX"
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_225(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_226(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "DEALERS SHORT GAMMA WILL AMPLIFY MOVES IN BOTH DIRECTIONS — VOLATILITY UNPINNED. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_227(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "XXPut dominance without unusual call activity signals no institutional accumulation. XX"
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_228(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_229(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "PUT DOMINANCE WITHOUT UNUSUAL CALL ACTIVITY SIGNALS NO INSTITUTIONAL ACCUMULATION. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_230(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "XXApplying −5pp confidence haircut.XX"
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_231(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_232(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "APPLYING −5PP CONFIDENCE HAIRCUT."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_233(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "XXsentimentXX": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_234(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "SENTIMENT": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_235(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "XXnegXX",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_236(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "NEG",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_237(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "XXmetaXX": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_238(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "META": f"pc_ratio={float(pc):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

    def xǁOptionsFlowConfirmationGateǁapply__mutmut_239(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or not ctx.has_mr or not ctx.opt_flow:
            return

        pc = ctx.opt_flow.get("pc_ratio")
        gex = float(ctx.opt_flow.get("gex") or 0.0)
        uv = ctx.opt_flow.get("unusual_vol_ratio")
        uv_s = f"{float(uv):.2f}" if uv is not None else "n/a"
        sweep_calls = bool(ctx.opt_flow.get("sweep_calls"))

        # Hard block: professional put hedging still building
        if pc is not None and float(pc) > 2.0:
            ctx.action = "HOLD"
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Extreme Put Dominance — Options Flow Blocks MR Entry (P/C {float(pc):.2f})",
                    "body": (
                        f"Put/call ratio of {float(pc):.2f} across near-term expiries signals "
                        "institutional hedging at scale, not retail panic. When professionals are "
                        "buying puts this aggressively on an oversold stock, they expect further "
                        "downside — not a bounce. MR setups require fear exhaustion; extreme put "
                        "flow confirms the fear is still building."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(pc):.2f} > 2.0 | mr_entry=True | options_flow_gate=True",
                }
            )
            return

        # Highest-conviction: call sweep + positive GEX simultaneously
        if sweep_calls and gex > 0:
            ctx.confidence = round(min(95.0, ctx.confidence + 15), 1)
            ctx.sources.add("Options")
            gex_str = f"${gex / 1e6:.1f}M" if abs(gex) >= 1e6 else f"${gex:.0f}"
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": "Call Sweep + Positive GEX — Highest-Conviction MR Setup (+15pp)",
                    "body": (
                        f"Call sweep detected (large cross-exchange institutional order) with positive "
                        f"dealer GEX ({gex_str}). Call sweeps signal urgency — institutions are "
                        "accumulating aggressively, not passively. Positive GEX means dealers must "
                        "mechanically buy the dip to stay hedged, creating a structural support floor. "
                        "These two forces reinforce the MR bounce thesis."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_calls=True gex={gex:.0f} | mr_entry=True | sweep_gex_combo=True | bonus=+15pp",
                }
            )
            return

        # Standard positive confirmation: positive GEX + call-dominant
        if pc is not None and gex > 0 and float(pc) < 0.75:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Options Flow Confirms MR Setup (P/C {float(pc):.2f}, Positive GEX)",
                    "body": (
                        f"Positive dealer GEX (${gex / 1e6:.1f}M) with call-dominant flow "
                        f"(P/C {float(pc):.2f}): dealers are net long gamma and must buy stock as "
                        "it dips — creating a mechanical support floor. Call dominance confirms "
                        "institutional accumulation, not distribution."
                    ),
                    "sentiment": "pos",
                    "meta": f"pc_ratio={float(pc):.2f} gex={gex:.0f} | mr_entry=True | options_flow_confirmed=True",
                }
            )
            return

        # Unconfirmed: negative GEX + put dominance + low unusual activity
        if pc is not None and gex < 0 and float(pc) > 1.5 and (uv is None or float(uv) < 0.5):
            ctx.confidence = round(max(35.0, ctx.confidence - 5), 1)
            ctx.sources.add("Risk Gate")
            ctx.rationale.append(
                {
                    "src": "Risk Gate",
                    "head": f"Options Flow Unconfirmed — Negative GEX + Puts Dominating (P/C {float(pc):.2f})",
                    "body": (
                        f"Negative dealer GEX (${abs(gex) / 1e6:.1f}M) with put-dominant flow "
                        f"(P/C {float(pc):.2f}) and low unusual activity ratio ({uv_s}). "
                        "Dealers short gamma will amplify moves in both directions — volatility unpinned. "
                        "Put dominance without unusual call activity signals no institutional accumulation. "
                        "Applying −5pp confidence haircut."
                    ),
                    "sentiment": "neg",
                    "meta": f"pc_ratio={float(None):.2f} gex={gex:.0f} uv={uv_s} | options_unconfirmed=True",
                }
            )

mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['_mutmut_orig'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_orig # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_1'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_1 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_2'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_2 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_3'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_3 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_4'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_4 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_5'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_5 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_6'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_6 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_7'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_7 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_8'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_8 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_9'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_9 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_10'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_10 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_11'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_11 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_12'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_12 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_13'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_13 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_14'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_14 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_15'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_15 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_16'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_16 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_17'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_17 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_18'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_18 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_19'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_19 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_20'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_20 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_21'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_21 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_22'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_22 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_23'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_23 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_24'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_24 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_25'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_25 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_26'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_26 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_27'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_27 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_28'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_28 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_29'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_29 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_30'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_30 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_31'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_31 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_32'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_32 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_33'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_33 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_34'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_34 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_35'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_35 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_36'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_36 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_37'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_37 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_38'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_38 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_39'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_39 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_40'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_40 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_41'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_41 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_42'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_42 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_43'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_43 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_44'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_44 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_45'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_45 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_46'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_46 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_47'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_47 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_48'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_48 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_49'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_49 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_50'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_50 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_51'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_51 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_52'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_52 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_53'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_53 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_54'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_54 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_55'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_55 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_56'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_56 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_57'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_57 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_58'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_58 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_59'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_59 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_60'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_60 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_61'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_61 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_62'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_62 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_63'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_63 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_64'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_64 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_65'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_65 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_66'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_66 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_67'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_67 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_68'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_68 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_69'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_69 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_70'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_70 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_71'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_71 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_72'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_72 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_73'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_73 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_74'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_74 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_75'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_75 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_76'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_76 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_77'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_77 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_78'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_78 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_79'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_79 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_80'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_80 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_81'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_81 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_82'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_82 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_83'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_83 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_84'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_84 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_85'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_85 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_86'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_86 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_87'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_87 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_88'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_88 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_89'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_89 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_90'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_90 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_91'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_91 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_92'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_92 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_93'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_93 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_94'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_94 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_95'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_95 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_96'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_96 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_97'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_97 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_98'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_98 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_99'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_99 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_100'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_100 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_101'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_101 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_102'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_102 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_103'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_103 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_104'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_104 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_105'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_105 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_106'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_106 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_107'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_107 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_108'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_108 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_109'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_109 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_110'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_110 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_111'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_111 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_112'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_112 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_113'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_113 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_114'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_114 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_115'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_115 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_116'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_116 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_117'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_117 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_118'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_118 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_119'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_119 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_120'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_120 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_121'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_121 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_122'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_122 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_123'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_123 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_124'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_124 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_125'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_125 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_126'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_126 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_127'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_127 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_128'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_128 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_129'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_129 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_130'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_130 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_131'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_131 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_132'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_132 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_133'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_133 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_134'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_134 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_135'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_135 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_136'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_136 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_137'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_137 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_138'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_138 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_139'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_139 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_140'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_140 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_141'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_141 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_142'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_142 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_143'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_143 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_144'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_144 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_145'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_145 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_146'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_146 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_147'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_147 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_148'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_148 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_149'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_149 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_150'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_150 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_151'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_151 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_152'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_152 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_153'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_153 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_154'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_154 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_155'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_155 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_156'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_156 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_157'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_157 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_158'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_158 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_159'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_159 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_160'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_160 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_161'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_161 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_162'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_162 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_163'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_163 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_164'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_164 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_165'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_165 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_166'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_166 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_167'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_167 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_168'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_168 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_169'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_169 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_170'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_170 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_171'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_171 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_172'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_172 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_173'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_173 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_174'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_174 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_175'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_175 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_176'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_176 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_177'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_177 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_178'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_178 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_179'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_179 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_180'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_180 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_181'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_181 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_182'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_182 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_183'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_183 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_184'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_184 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_185'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_185 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_186'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_186 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_187'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_187 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_188'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_188 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_189'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_189 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_190'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_190 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_191'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_191 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_192'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_192 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_193'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_193 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_194'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_194 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_195'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_195 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_196'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_196 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_197'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_197 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_198'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_198 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_199'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_199 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_200'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_200 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_201'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_201 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_202'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_202 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_203'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_203 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_204'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_204 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_205'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_205 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_206'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_206 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_207'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_207 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_208'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_208 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_209'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_209 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_210'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_210 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_211'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_211 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_212'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_212 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_213'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_213 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_214'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_214 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_215'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_215 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_216'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_216 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_217'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_217 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_218'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_218 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_219'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_219 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_220'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_220 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_221'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_221 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_222'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_222 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_223'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_223 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_224'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_224 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_225'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_225 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_226'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_226 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_227'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_227 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_228'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_228 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_229'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_229 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_230'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_230 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_231'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_231 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_232'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_232 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_233'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_233 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_234'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_234 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_235'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_235 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_236'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_236 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_237'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_237 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_238'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_238 # type: ignore # mutmut generated
mutants_xǁOptionsFlowConfirmationGateǁapply__mutmut['xǁOptionsFlowConfirmationGateǁapply__mutmut_239'] = OptionsFlowConfirmationGate.xǁOptionsFlowConfirmationGateǁapply__mutmut_239 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut: MutantDict = {}  # type: ignore


class IvRankFlagGate(GateBase):
    """
    Informational IV rank disclosure — no score/confidence change.

    IV Rank > 70 flags expensive options premium and post-earnings IV crush
    risk for the trader.  Fires regardless of has_mr.
    """

    @_mutmut_mutated(mutants_xǁIvRankFlagGateǁapply__mutmut)
    def apply(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_orig(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_1(self, ctx: SignalContext) -> None:
        if ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_2(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = None
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_3(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get(None)
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_4(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("XXiv_rankXX")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_5(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("IV_RANK")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_6(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None and float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_7(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is not None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_8(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(None) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_9(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) < 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_10(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 71:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_11(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = None
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_12(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get(None)
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_13(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("XXdays_since_earningsXX")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_14(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("DAYS_SINCE_EARNINGS")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_15(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = None
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_16(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get(None)
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_17(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("XXdays_to_earningsXX")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_18(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("DAYS_TO_EARNINGS")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_19(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = None
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_20(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) and (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_21(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None or 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_22(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_23(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 1 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_24(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 < days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_25(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since < 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_26(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 8) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_27(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None or 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_28(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_29(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 1 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_30(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 < days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_31(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to < 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_32(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 15
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_33(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = None

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_34(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(None)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_35(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None or 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_36(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_37(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 1 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_38(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 < (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_39(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since and 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_40(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 1000) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_41(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) < 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_42(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 8:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_43(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = None
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_44(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = None
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_45(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 + ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_46(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {101 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_47(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "XX— options premium buyers lose value even on correct directional moves. XX"
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_48(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— OPTIONS PREMIUM BUYERS LOSE VALUE EVEN ON CORRECT DIRECTIONAL MOVES. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_49(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "XXFavour stock entry over options; size equity positions accordingly.XX"
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_50(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_51(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "FAVOUR STOCK ENTRY OVER OPTIONS; SIZE EQUITY POSITIONS ACCORDINGLY."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_52(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = None
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_53(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = None

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_54(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "XXBuying calls or puts here means paying rich premium — a significant move is needed just XX"
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_55(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_56(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "BUYING CALLS OR PUTS HERE MEANS PAYING RICH PREMIUM — A SIGNIFICANT MOVE IS NEEDED JUST "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_57(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "XXto break even. Consider stock entry instead of options, or wait for IV to normalise.XX"
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_58(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. consider stock entry instead of options, or wait for iv to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_59(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "TO BREAK EVEN. CONSIDER STOCK ENTRY INSTEAD OF OPTIONS, OR WAIT FOR IV TO NORMALISE."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_60(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add(None)
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_61(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("XXOptionsXX")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_62(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_63(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("OPTIONS")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_64(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            None
        )

    def xǁIvRankFlagGateǁapply__mutmut_65(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "XXsrcXX": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_66(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "SRC": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_67(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "XXOptionsXX",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_68(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_69(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "OPTIONS",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_70(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "XXheadXX": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_71(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "HEAD": head,
                "body": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_72(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "XXbodyXX": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_73(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "BODY": body,
                "sentiment": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_74(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "XXsentimentXX": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_75(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "SENTIMENT": "neg",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_76(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "XXnegXX",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_77(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "NEG",
                "meta": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_78(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "XXmetaXX": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

    def xǁIvRankFlagGateǁapply__mutmut_79(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow:
            return
        iv_rank = ctx.opt_flow.get("iv_rank")
        if iv_rank is None or float(iv_rank) <= 70:
            return

        days_since = ctx.earnings_cal.get("days_since_earnings")
        days_to = ctx.earnings_cal.get("days_to_earnings")
        near_earnings = (days_since is not None and 0 <= days_since <= 7) or (
            days_to is not None and 0 <= days_to <= 14
        )
        ivr = float(iv_rank)

        if days_since is not None and 0 <= (days_since or 999) <= 7:
            head = f"Elevated IV Rank {ivr:.0f} — Post-Earnings IV Crush Risk"
            body = (
                f"IV Rank of {ivr:.0f} (top {100 - ivr:.0f}% of trailing year) "
                f"with earnings {days_since}d ago: implied volatility will deflate rapidly (IV crush) "
                "— options premium buyers lose value even on correct directional moves. "
                "Favour stock entry over options; size equity positions accordingly."
            )
        else:
            head = f"Elevated IV Rank {ivr:.0f} — Options Premium Is Expensive"
            body = (
                f"IV Rank of {ivr:.0f} means implied volatility is elevated relative to the past year. "
                "Buying calls or puts here means paying rich premium — a significant move is needed just "
                "to break even. Consider stock entry instead of options, or wait for IV to normalise."
            )

        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": head,
                "body": body,
                "sentiment": "neg",
                "META": f"iv_rank={ivr:.1f} | near_earnings={near_earnings}",
            }
        )

mutants_xǁIvRankFlagGateǁapply__mutmut['_mutmut_orig'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_orig # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_1'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_1 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_2'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_2 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_3'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_3 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_4'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_4 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_5'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_5 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_6'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_6 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_7'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_7 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_8'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_8 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_9'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_9 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_10'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_10 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_11'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_11 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_12'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_12 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_13'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_13 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_14'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_14 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_15'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_15 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_16'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_16 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_17'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_17 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_18'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_18 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_19'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_19 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_20'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_20 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_21'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_21 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_22'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_22 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_23'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_23 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_24'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_24 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_25'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_25 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_26'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_26 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_27'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_27 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_28'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_28 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_29'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_29 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_30'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_30 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_31'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_31 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_32'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_32 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_33'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_33 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_34'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_34 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_35'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_35 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_36'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_36 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_37'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_37 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_38'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_38 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_39'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_39 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_40'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_40 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_41'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_41 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_42'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_42 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_43'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_43 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_44'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_44 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_45'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_45 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_46'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_46 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_47'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_47 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_48'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_48 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_49'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_49 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_50'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_50 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_51'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_51 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_52'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_52 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_53'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_53 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_54'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_54 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_55'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_55 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_56'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_56 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_57'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_57 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_58'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_58 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_59'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_59 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_60'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_60 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_61'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_61 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_62'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_62 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_63'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_63 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_64'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_64 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_65'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_65 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_66'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_66 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_67'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_67 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_68'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_68 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_69'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_69 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_70'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_70 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_71'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_71 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_72'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_72 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_73'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_73 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_74'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_74 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_75'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_75 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_76'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_76 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_77'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_77 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_78'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_78 # type: ignore # mutmut generated
mutants_xǁIvRankFlagGateǁapply__mutmut['xǁIvRankFlagGateǁapply__mutmut_79'] = IvRankFlagGate.xǁIvRankFlagGateǁapply__mutmut_79 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut: MutantDict = {}  # type: ignore


class IvrMrGate(GateBase):
    """
    §48 IVR MR amplifier — dealer hedge unwind.

    High IVR with an oversold MR setup signals acute dealer hedging that will
    unwind sharply when panic exhausts (+5 raw score).  Low IVR means the
    dealer-pressure effect is absent (−3 raw score).

    Note: modifies ctx.score (not confidence) because _score_to_action() has
    already been called; the adjustment feeds raw_score in the final signal dict.
    """

    @_mutmut_mutated(mutants_xǁIvrMrGateǁapply__mutmut)
    def apply(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_orig(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_1(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow and not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_2(self, ctx: SignalContext) -> None:
        if ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_3(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_4(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action == "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_5(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "XXBUYXX":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_6(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "buy":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_7(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None and ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_8(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is not None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_9(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix < 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_10(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 16:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_11(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = None
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_12(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get(None)
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_13(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("XXiv_rankXX")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_14(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("IV_RANK")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_15(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is not None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_16(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = None

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_17(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(None)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_18(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f > 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_19(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 51:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_20(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score = 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_21(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score -= 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_22(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 6
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_23(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add(None)
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_24(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("XXOptionsXX")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_25(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_26(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("OPTIONS")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_27(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                None
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_28(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "XXsrcXX": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_29(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "SRC": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_30(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "XXOptionsXX",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_31(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_32(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "OPTIONS",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_33(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "XXheadXX": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_34(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "HEAD": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_35(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "XXbodyXX": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_36(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "BODY": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_37(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "XXpositions must delta-hedge by selling stock as it falls — adding to the XX"
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_38(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "POSITIONS MUST DELTA-HEDGE BY SELLING STOCK AS IT FALLS — ADDING TO THE "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_39(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "XXforced selling. When panic exhausts, the entire hedge unwinds (buy pressure). XX"
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_40(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. when panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_41(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "FORCED SELLING. WHEN PANIC EXHAUSTS, THE ENTIRE HEDGE UNWINDS (BUY PRESSURE). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_42(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "XXHigh IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). XX"
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_43(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "high ivr mr setups have historically shown 74% wr (cracking markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_44(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "HIGH IVR MR SETUPS HAVE HISTORICALLY SHOWN 74% WR (CRACKING MARKETS 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_45(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "XXConfidence boosted +5pp (§48).XX"
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_46(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_47(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "CONFIDENCE BOOSTED +5PP (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_48(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "XXsentimentXX": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_49(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "SENTIMENT": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_50(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "XXposXX",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_51(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "POS",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_52(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "XXmetaXX": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_53(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "META": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_54(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f <= 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_55(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 21:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_56(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score = 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_57(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score += 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_58(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_59(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add(None)
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_60(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("XXOptionsXX")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_61(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_62(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("OPTIONS")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_63(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                None
            )

    def xǁIvrMrGateǁapply__mutmut_64(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "XXsrcXX": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_65(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "SRC": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_66(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "XXOptionsXX",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_67(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_68(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "OPTIONS",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_69(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "XXheadXX": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_70(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "HEAD": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_71(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "XXbodyXX": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_72(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "BODY": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_73(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "XXDealers carry minimal hedge books; the unwind amplification effect is absent. XX"
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_74(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_75(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "DEALERS CARRY MINIMAL HEDGE BOOKS; THE UNWIND AMPLIFICATION EFFECT IS ABSENT. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_76(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "XXLow-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. XX"
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_77(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "low-ivr mr setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_78(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "LOW-IVR MR SETUPS LACK THE DEALER-PRESSURE ENERGY THAT DRIVES SHARP REVERSALS. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_79(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "XXConfidence reduced −3pp (§48).XX"
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_80(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_81(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "CONFIDENCE REDUCED −3PP (§48)."
                    ),
                    "sentiment": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_82(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "XXsentimentXX": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_83(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "SENTIMENT": "neg",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_84(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "XXnegXX",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_85(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "NEG",
                    "meta": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_86(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "XXmetaXX": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

    def xǁIvrMrGateǁapply__mutmut_87(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr:
            return
        if ctx.action != "BUY":
            return
        if ctx.vix is None or ctx.vix <= 15:
            return

        ivr = ctx.opt_flow.get("iv_rank")
        if ivr is None:
            return
        ivr_f = float(ivr)

        if ivr_f >= 50:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High IVR {ivr_f:.0f} — Dealer Hedge Unwind Amplifies Recovery (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% with an oversold MR setup. Dealers short large put "
                        "positions must delta-hedge by selling stock as it falls — adding to the "
                        "forced selling. When panic exhausts, the entire hedge unwinds (buy pressure). "
                        "High IVR MR setups have historically shown 74% WR (Cracking Markets 2020–2025). "
                        "Confidence boosted +5pp (§48)."
                    ),
                    "sentiment": "pos",
                    "meta": f"ivr_mr={ivr_f:.0f} vix={ctx.vix:.1f} dealer_unwind_setup=True §48",
                }
            )
        elif ivr_f < 20:
            ctx.score -= 3
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Low IVR {ivr_f:.0f} — Calm Drift, No Dealer Pressure (§48)",
                    "body": (
                        f"IV Rank {ivr_f:.0f}% — implied volatility is historically low. "
                        "Dealers carry minimal hedge books; the unwind amplification effect is absent. "
                        "Low-IVR MR setups lack the dealer-pressure energy that drives sharp reversals. "
                        "Confidence reduced −3pp (§48)."
                    ),
                    "sentiment": "neg",
                    "META": f"ivr_mr={ivr_f:.0f} low_dealer_pressure §48",
                }
            )

mutants_xǁIvrMrGateǁapply__mutmut['_mutmut_orig'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_orig # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_1'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_1 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_2'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_2 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_3'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_3 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_4'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_4 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_5'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_5 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_6'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_6 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_7'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_7 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_8'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_8 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_9'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_9 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_10'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_10 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_11'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_11 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_12'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_12 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_13'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_13 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_14'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_14 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_15'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_15 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_16'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_16 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_17'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_17 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_18'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_18 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_19'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_19 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_20'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_20 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_21'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_21 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_22'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_22 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_23'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_23 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_24'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_24 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_25'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_25 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_26'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_26 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_27'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_27 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_28'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_28 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_29'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_29 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_30'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_30 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_31'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_31 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_32'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_32 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_33'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_33 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_34'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_34 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_35'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_35 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_36'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_36 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_37'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_37 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_38'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_38 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_39'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_39 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_40'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_40 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_41'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_41 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_42'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_42 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_43'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_43 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_44'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_44 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_45'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_45 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_46'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_46 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_47'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_47 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_48'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_48 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_49'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_49 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_50'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_50 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_51'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_51 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_52'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_52 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_53'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_53 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_54'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_54 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_55'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_55 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_56'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_56 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_57'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_57 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_58'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_58 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_59'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_59 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_60'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_60 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_61'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_61 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_62'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_62 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_63'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_63 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_64'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_64 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_65'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_65 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_66'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_66 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_67'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_67 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_68'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_68 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_69'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_69 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_70'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_70 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_71'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_71 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_72'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_72 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_73'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_73 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_74'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_74 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_75'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_75 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_76'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_76 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_77'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_77 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_78'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_78 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_79'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_79 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_80'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_80 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_81'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_81 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_82'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_82 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_83'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_83 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_84'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_84 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_85'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_85 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_86'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_86 # type: ignore # mutmut generated
mutants_xǁIvrMrGateǁapply__mutmut['xǁIvrMrGateǁapply__mutmut_87'] = IvrMrGate.xǁIvrMrGateǁapply__mutmut_87 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut: MutantDict = {}  # type: ignore


class PutCallSkewGate(GateBase):
    """
    §49 Put-call skew — panic precision signal.

    High 25d put skew (put IV >> call IV) forces dealers to over-hedge via
    delta-selling.  When panic exhausts, put IV collapses and hedges unwind
    simultaneously → sharpest MR snap-backs (+4 raw score).
    """

    @_mutmut_mutated(mutants_xǁPutCallSkewGateǁapply__mutmut)
    def apply(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_orig(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_1(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr and ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_2(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow and not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_3(self, ctx: SignalContext) -> None:
        if ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_4(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_5(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action == "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_6(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "XXBUYXX":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_7(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "buy":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_8(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = None
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_9(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get(None)
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_10(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("XXskew_25dXX")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_11(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("SKEW_25D")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_12(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is not None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_13(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = None
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_14(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(None)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_15(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f >= 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_16(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 1.1:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_17(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score = 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_18(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score -= 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_19(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 5
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_20(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add(None)
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_21(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("XXOptionsXX")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_22(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_23(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("OPTIONS")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_24(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                None
            )

    def xǁPutCallSkewGateǁapply__mutmut_25(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "XXsrcXX": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_26(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "SRC": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_27(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "XXOptionsXX",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_28(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_29(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "OPTIONS",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_30(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "XXheadXX": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_31(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "HEAD": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_32(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f / 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_33(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 101:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_34(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "XXbodyXX": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_35(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "BODY": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_36(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f / 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_37(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 101:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_38(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "XXExtreme put skew isolates pure downside panic (not general fear). XX"
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_39(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_40(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "EXTREME PUT SKEW ISOLATES PURE DOWNSIDE PANIC (NOT GENERAL FEAR). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_41(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "XXAs panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind XX"
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_42(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "as panic exhausts, put iv collapses (iv crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_43(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "AS PANIC EXHAUSTS, PUT IV COLLAPSES (IV CRUSH) AND DEALER PUT HEDGES UNWIND "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_44(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "XX(forced buying). 25d put/call ratio >1.20 historically signals MR entries XX"
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_45(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals mr entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_46(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(FORCED BUYING). 25D PUT/CALL RATIO >1.20 HISTORICALLY SIGNALS MR ENTRIES "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_47(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "XXwith 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49).XX"
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_48(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% wr vs 54% in flat-skew setups (arxiv 2016). confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_49(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "WITH 68% WR VS 54% IN FLAT-SKEW SETUPS (ARXIV 2016). CONFIDENCE +4PP (§49)."
                    ),
                    "sentiment": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_50(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "XXsentimentXX": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_51(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "SENTIMENT": "pos",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_52(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "XXposXX",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_53(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "POS",
                    "meta": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_54(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "XXmetaXX": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

    def xǁPutCallSkewGateǁapply__mutmut_55(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        skew = ctx.opt_flow.get("skew_25d")
        if skew is None:
            return
        skew_f = float(skew)
        if skew_f > 0.10:
            ctx.score += 4
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"High Put Skew ({skew_f * 100:.0f}pp) — Panic Peak MR Signal (§49)",
                    "body": (
                        f"25-delta puts carry {skew_f * 100:.0f}pp more IV than equivalent calls. "
                        "Extreme put skew isolates pure downside panic (not general fear). "
                        "As panic exhausts, put IV collapses (IV crush) and dealer put hedges unwind "
                        "(forced buying). 25d put/call ratio >1.20 historically signals MR entries "
                        "with 68% WR vs 54% in flat-skew setups (arXiv 2016). Confidence +4pp (§49)."
                    ),
                    "sentiment": "pos",
                    "META": f"skew_25d={skew_f:.3f} panic_peak_mr §49",
                }
            )

mutants_xǁPutCallSkewGateǁapply__mutmut['_mutmut_orig'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_orig # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_1'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_1 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_2'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_2 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_3'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_3 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_4'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_4 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_5'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_5 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_6'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_6 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_7'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_7 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_8'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_8 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_9'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_9 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_10'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_10 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_11'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_11 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_12'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_12 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_13'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_13 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_14'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_14 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_15'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_15 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_16'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_16 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_17'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_17 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_18'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_18 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_19'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_19 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_20'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_20 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_21'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_21 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_22'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_22 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_23'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_23 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_24'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_24 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_25'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_25 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_26'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_26 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_27'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_27 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_28'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_28 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_29'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_29 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_30'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_30 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_31'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_31 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_32'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_32 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_33'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_33 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_34'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_34 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_35'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_35 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_36'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_36 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_37'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_37 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_38'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_38 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_39'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_39 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_40'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_40 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_41'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_41 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_42'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_42 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_43'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_43 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_44'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_44 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_45'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_45 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_46'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_46 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_47'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_47 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_48'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_48 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_49'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_49 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_50'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_50 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_51'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_51 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_52'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_52 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_53'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_53 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_54'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_54 # type: ignore # mutmut generated
mutants_xǁPutCallSkewGateǁapply__mutmut['xǁPutCallSkewGateǁapply__mutmut_55'] = PutCallSkewGate.xǁPutCallSkewGateǁapply__mutmut_55 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut: MutantDict = {}  # type: ignore


class IvTermStructureGate(GateBase):
    """
    IV term structure spike — MR amplifier.

    Near-term IV >> back-month (ratio > 1.5) means dealers are at peak hedging.
    The snap-back when exhaustion hits will be sharpest (+4 raw score).
    """

    @_mutmut_mutated(mutants_xǁIvTermStructureGateǁapply__mutmut)
    def apply(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_orig(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_1(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr and ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_2(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow and not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_3(self, ctx: SignalContext) -> None:
        if ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_4(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_5(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action == "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_6(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "XXBUYXX":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_7(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "buy":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_8(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = None
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_9(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get(None)
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_10(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("XXiv_term_spikeXX")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_11(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("IV_TERM_SPIKE")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_12(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None and float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_13(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is not None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_14(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(None) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_15(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) < 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_16(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 2.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_17(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = None
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_18(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(None)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_19(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score = 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_20(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score -= 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_21(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 5
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_22(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add(None)
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_23(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("XXOptionsXX")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_24(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_25(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("OPTIONS")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_26(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            None
        )

    def xǁIvTermStructureGateǁapply__mutmut_27(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "XXsrcXX": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_28(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "SRC": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_29(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "XXOptionsXX",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_30(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_31(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "OPTIONS",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_32(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "XXheadXX": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_33(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "HEAD": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_34(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "XXbodyXX": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_35(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "BODY": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_36(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "XXacute near-term panic. Dealers are at their most heavily hedged right now. XX"
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_37(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_38(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "ACUTE NEAR-TERM PANIC. DEALERS ARE AT THEIR MOST HEAVILY HEDGED RIGHT NOW. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_39(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "XXWhen this panic exhausts, the hedge unwind (forced dealer buying) will be XX"
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_40(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "when this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_41(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "WHEN THIS PANIC EXHAUSTS, THE HEDGE UNWIND (FORCED DEALER BUYING) WILL BE "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_42(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "XXexceptionally sharp. IV term spikes of this magnitude historically precede the XX"
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_43(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. iv term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_44(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "EXCEPTIONALLY SHARP. IV TERM SPIKES OF THIS MAGNITUDE HISTORICALLY PRECEDE THE "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_45(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "XXstrongest MR snap-backs. Confidence +4pp.XX"
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_46(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest mr snap-backs. confidence +4pp."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_47(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "STRONGEST MR SNAP-BACKS. CONFIDENCE +4PP."
                ),
                "sentiment": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_48(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "XXsentimentXX": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_49(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "SENTIMENT": "pos",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_50(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "XXposXX",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_51(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "POS",
                "meta": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_52(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "XXmetaXX": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

    def xǁIvTermStructureGateǁapply__mutmut_53(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        spike = ctx.opt_flow.get("iv_term_spike")
        if spike is None or float(spike) <= 1.5:
            return
        spike_f = float(spike)
        ctx.score += 4
        ctx.sources.add("Options")
        ctx.rationale.append(
            {
                "src": "Options",
                "head": f"Near-term IV Spike ({spike_f:.1f}×) — Acute Panic, MR Amplifier",
                "body": (
                    f"Short-dated IV is {spike_f:.1f}× back-month — the market is pricing "
                    "acute near-term panic. Dealers are at their most heavily hedged right now. "
                    "When this panic exhausts, the hedge unwind (forced dealer buying) will be "
                    "exceptionally sharp. IV term spikes of this magnitude historically precede the "
                    "strongest MR snap-backs. Confidence +4pp."
                ),
                "sentiment": "pos",
                "META": f"iv_term_spike={spike_f:.2f} mr_amplifier=True",
            }
        )

mutants_xǁIvTermStructureGateǁapply__mutmut['_mutmut_orig'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_orig # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_1'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_1 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_2'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_2 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_3'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_3 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_4'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_4 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_5'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_5 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_6'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_6 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_7'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_7 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_8'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_8 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_9'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_9 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_10'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_10 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_11'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_11 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_12'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_12 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_13'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_13 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_14'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_14 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_15'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_15 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_16'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_16 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_17'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_17 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_18'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_18 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_19'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_19 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_20'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_20 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_21'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_21 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_22'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_22 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_23'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_23 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_24'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_24 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_25'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_25 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_26'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_26 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_27'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_27 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_28'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_28 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_29'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_29 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_30'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_30 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_31'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_31 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_32'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_32 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_33'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_33 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_34'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_34 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_35'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_35 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_36'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_36 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_37'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_37 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_38'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_38 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_39'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_39 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_40'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_40 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_41'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_41 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_42'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_42 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_43'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_43 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_44'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_44 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_45'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_45 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_46'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_46 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_47'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_47 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_48'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_48 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_49'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_49 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_50'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_50 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_51'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_51 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_52'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_52 # type: ignore # mutmut generated
mutants_xǁIvTermStructureGateǁapply__mutmut['xǁIvTermStructureGateǁapply__mutmut_53'] = IvTermStructureGate.xǁIvTermStructureGateǁapply__mutmut_53 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut: MutantDict = {}  # type: ignore


class PutSweepCapitulationGate(GateBase):
    """
    Extreme put sweep capitulation confirmation.

    A put sweep with vol/OI > 10× and volume > 1000 contracts (with NO call
    sweeps present) is the signature of forced protection buying at capitulation,
    not informed institutional shorting.  Confidence +5pp.
    """

    @_mutmut_mutated(mutants_xǁPutSweepCapitulationGateǁapply__mutmut)
    def apply(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_orig(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_1(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr and ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_2(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow and not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_3(self, ctx: SignalContext) -> None:
        if ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_4(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_5(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action == "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_6(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "XXBUYXX":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_7(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "buy":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_8(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = None
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_9(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") and []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_10(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get(None) or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_11(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("XXsweep_putsXX") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_12(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("SWEEP_PUTS") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_13(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = None
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_14(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") and []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_15(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get(None) or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_16(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("XXsweep_callsXX") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_17(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("SWEEP_CALLS") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_18(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts and sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_19(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_20(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = None
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_21(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[1]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_22(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = None
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_23(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(None)
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_24(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get(None, 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_25(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", None))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_26(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get(0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_27(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", ))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_28(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("XXvol_oiXX", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_29(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("VOL_OI", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_30(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 1))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_31(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = None
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_32(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(None)
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_33(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get(None, 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_34(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", None))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_35(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get(0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_36(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", ))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_37(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("XXvolXX", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_38(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("VOL", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_39(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 1))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_40(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 or vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_41(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi >= 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_42(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 11 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_43(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol >= 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_44(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1001:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_45(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = None
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_46(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(None, 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_47(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), None)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_48(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_49(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), )
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_50(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(None, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_51(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, None), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_52(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_53(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_54(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(96.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_55(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence - 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_56(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 6), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_57(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 2)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_58(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add(None)
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_59(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("XXOptionsXX")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_60(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_61(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("OPTIONS")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_62(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                None
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_63(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "XXsrcXX": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_64(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "SRC": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_65(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "XXOptionsXX",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_66(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_67(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "OPTIONS",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_68(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "XXheadXX": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_69(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "HEAD": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_70(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "XXbodyXX": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_71(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "BODY": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_72(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get(None, 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_73(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', None):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_74(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get(0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_75(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', ):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_76(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('XXstrikeXX', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_77(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('STRIKE', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_78(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 1):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_79(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get(None, 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_80(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', None)}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_81(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_82(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', )}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_83(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('XXexpiryXX', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_84(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('EXPIRY', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_85(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'XXn/aXX')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_86(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'N/A')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_87(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "XXExtreme vol/OI ratio (>10×) with high volume is the signature of forced protection XX"
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_88(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "extreme vol/oi ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_89(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "EXTREME VOL/OI RATIO (>10×) WITH HIGH VOLUME IS THE SIGNATURE OF FORCED PROTECTION "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_90(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "XXbuying at capitulation — institutions covering longs in panic, not informed shorts XX"
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_91(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "BUYING AT CAPITULATION — INSTITUTIONS COVERING LONGS IN PANIC, NOT INFORMED SHORTS "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_92(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "XX(which use smaller iceberg-style orders). No offsetting call sweep present. XX"
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_93(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). no offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_94(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(WHICH USE SMALLER ICEBERG-STYLE ORDERS). NO OFFSETTING CALL SWEEP PRESENT. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_95(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "XXConfidence +5pp.XX"
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_96(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_97(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "CONFIDENCE +5PP."
                    ),
                    "sentiment": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_98(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "XXsentimentXX": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_99(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "SENTIMENT": "pos",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_100(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "XXposXX",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_101(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "POS",
                    "meta": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_102(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "XXmetaXX": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

    def xǁPutSweepCapitulationGateǁapply__mutmut_103(self, ctx: SignalContext) -> None:
        if not ctx.opt_flow or not ctx.has_mr or ctx.action != "BUY":
            return
        sweep_puts = ctx.opt_flow.get("sweep_puts") or []
        sweep_calls = ctx.opt_flow.get("sweep_calls") or []
        if not sweep_puts or sweep_calls:
            return
        top = sweep_puts[0]
        vol_oi = float(top.get("vol_oi", 0))
        vol = int(top.get("vol", 0))
        if vol_oi > 10 and vol > 1000:
            ctx.confidence = round(min(95.0, ctx.confidence + 5), 1)
            ctx.sources.add("Options")
            ctx.rationale.append(
                {
                    "src": "Options",
                    "head": f"Extreme Put Sweep ({vol:,} contracts, {vol_oi:.0f}× OI) — Capitulation Signal",
                    "body": (
                        f"Single-strike put sweep: {vol:,} contracts at {vol_oi:.0f}× open interest "
                        f"(strike ${top.get('strike', 0):.0f}, expiry {top.get('expiry', 'n/a')}). "
                        "Extreme vol/OI ratio (>10×) with high volume is the signature of forced protection "
                        "buying at capitulation — institutions covering longs in panic, not informed shorts "
                        "(which use smaller iceberg-style orders). No offsetting call sweep present. "
                        "Confidence +5pp."
                    ),
                    "sentiment": "pos",
                    "META": f"sweep_put_vol={vol} vol_oi={vol_oi:.1f}× capitulation_signal=True",
                }
            )

mutants_xǁPutSweepCapitulationGateǁapply__mutmut['_mutmut_orig'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_orig # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_1'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_1 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_2'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_2 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_3'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_3 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_4'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_4 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_5'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_5 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_6'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_6 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_7'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_7 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_8'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_8 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_9'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_9 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_10'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_10 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_11'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_11 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_12'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_12 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_13'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_13 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_14'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_14 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_15'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_15 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_16'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_16 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_17'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_17 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_18'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_18 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_19'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_19 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_20'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_20 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_21'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_21 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_22'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_22 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_23'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_23 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_24'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_24 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_25'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_25 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_26'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_26 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_27'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_27 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_28'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_28 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_29'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_29 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_30'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_30 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_31'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_31 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_32'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_32 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_33'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_33 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_34'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_34 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_35'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_35 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_36'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_36 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_37'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_37 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_38'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_38 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_39'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_39 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_40'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_40 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_41'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_41 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_42'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_42 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_43'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_43 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_44'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_44 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_45'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_45 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_46'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_46 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_47'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_47 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_48'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_48 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_49'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_49 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_50'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_50 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_51'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_51 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_52'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_52 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_53'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_53 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_54'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_54 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_55'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_55 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_56'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_56 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_57'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_57 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_58'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_58 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_59'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_59 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_60'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_60 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_61'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_61 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_62'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_62 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_63'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_63 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_64'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_64 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_65'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_65 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_66'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_66 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_67'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_67 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_68'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_68 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_69'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_69 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_70'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_70 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_71'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_71 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_72'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_72 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_73'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_73 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_74'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_74 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_75'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_75 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_76'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_76 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_77'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_77 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_78'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_78 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_79'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_79 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_80'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_80 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_81'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_81 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_82'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_82 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_83'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_83 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_84'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_84 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_85'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_85 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_86'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_86 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_87'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_87 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_88'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_88 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_89'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_89 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_90'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_90 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_91'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_91 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_92'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_92 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_93'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_93 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_94'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_94 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_95'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_95 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_96'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_96 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_97'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_97 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_98'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_98 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_99'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_99 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_100'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_100 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_101'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_101 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_102'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_102 # type: ignore # mutmut generated
mutants_xǁPutSweepCapitulationGateǁapply__mutmut['xǁPutSweepCapitulationGateǁapply__mutmut_103'] = PutSweepCapitulationGate.xǁPutSweepCapitulationGateǁapply__mutmut_103 # type: ignore # mutmut generated
