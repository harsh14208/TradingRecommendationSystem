"""
13F Institutional Flow — SEC EDGAR XBRL parsing.

Tracks position changes from top hedge funds and institutional investors in our watchlist.
Quarterly lag (~45 days after quarter end), but high-credibility signal.

Free data source: https://data.sec.gov/  (no API key required)
Rate limit: 10 req/sec per SEC fair use policy.
"""

import asyncio
import logging
import time

import aiohttp
from services.http_client import shared_session

log = logging.getLogger("signal.trade.institutional")

# Top institutional investors tracked — (name, CIK, tier)
# Tier: "tier1" = highest credibility (Berkshire, Bridgewater, etc.)
TRACKED_FUNDS = [
    ("Berkshire Hathaway", "0001067983", "tier1"),
    ("Vanguard Group", "0000102909", "tier1"),
    ("BlackRock", "0001364742", "tier1"),
    ("State Street", "0000093751", "tier1"),
    ("Fidelity", "0000315066", "tier1"),
    ("JPMorgan Asset Mgmt", "0000049196", "tier2"),
    ("Capital Group", "0000813672", "tier2"),
    ("Invesco", "0000914208", "tier2"),
    ("Viking Global", "0001111830", "tier1"),
    ("Tiger Global", "0001167483", "tier1"),
    ("Duquesne Family Office", "0001537721", "tier1"),
    ("Pershing Square", "0001336528", "tier1"),
    ("Baupost Group", "0001100663", "tier1"),
    ("Third Point", "0001040273", "tier2"),
    ("Elliott Management", "0000886871", "tier2"),
]

TIER_SCORES = {"tier1": 8, "tier2": 4}

_CACHE: dict = {}
_CACHE_TTL = 6 * 3600  # 6 hours — filings don't change intraday

_SEC_HEADERS = {
    "User-Agent": "Signal.Trade harsh13858@gmail.com",
    "Accept-Encoding": "gzip, deflate",
    "Host": "data.sec.gov",
}


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__get__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__get__mutmut)
async def _get(session: aiohttp.ClientSession, url: str) -> dict | list | None:
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
            if r.status == 200:
                return await r.json(content_type=None)
            log.debug(f"[13F] {url} → {r.status}")
            return None
    except Exception as e:
        log.debug(f"[13F] request failed {url}: {e}")
        return None


async def x__get__mutmut_orig(session: aiohttp.ClientSession, url: str) -> dict | list | None:
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
            if r.status == 200:
                return await r.json(content_type=None)
            log.debug(f"[13F] {url} → {r.status}")
            return None
    except Exception as e:
        log.debug(f"[13F] request failed {url}: {e}")
        return None


async def x__get__mutmut_1(session: aiohttp.ClientSession, url: str) -> dict | list | None:
    try:
        async with session.get(None, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
            if r.status == 200:
                return await r.json(content_type=None)
            log.debug(f"[13F] {url} → {r.status}")
            return None
    except Exception as e:
        log.debug(f"[13F] request failed {url}: {e}")
        return None


async def x__get__mutmut_2(session: aiohttp.ClientSession, url: str) -> dict | list | None:
    try:
        async with session.get(url, headers=None, timeout=aiohttp.ClientTimeout(total=15)) as r:
            if r.status == 200:
                return await r.json(content_type=None)
            log.debug(f"[13F] {url} → {r.status}")
            return None
    except Exception as e:
        log.debug(f"[13F] request failed {url}: {e}")
        return None


async def x__get__mutmut_3(session: aiohttp.ClientSession, url: str) -> dict | list | None:
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=None) as r:
            if r.status == 200:
                return await r.json(content_type=None)
            log.debug(f"[13F] {url} → {r.status}")
            return None
    except Exception as e:
        log.debug(f"[13F] request failed {url}: {e}")
        return None


async def x__get__mutmut_4(session: aiohttp.ClientSession, url: str) -> dict | list | None:
    try:
        async with session.get(headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
            if r.status == 200:
                return await r.json(content_type=None)
            log.debug(f"[13F] {url} → {r.status}")
            return None
    except Exception as e:
        log.debug(f"[13F] request failed {url}: {e}")
        return None


async def x__get__mutmut_5(session: aiohttp.ClientSession, url: str) -> dict | list | None:
    try:
        async with session.get(url, timeout=aiohttp.ClientTimeout(total=15)) as r:
            if r.status == 200:
                return await r.json(content_type=None)
            log.debug(f"[13F] {url} → {r.status}")
            return None
    except Exception as e:
        log.debug(f"[13F] request failed {url}: {e}")
        return None


async def x__get__mutmut_6(session: aiohttp.ClientSession, url: str) -> dict | list | None:
    try:
        async with session.get(url, headers=_SEC_HEADERS, ) as r:
            if r.status == 200:
                return await r.json(content_type=None)
            log.debug(f"[13F] {url} → {r.status}")
            return None
    except Exception as e:
        log.debug(f"[13F] request failed {url}: {e}")
        return None


async def x__get__mutmut_7(session: aiohttp.ClientSession, url: str) -> dict | list | None:
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=None)) as r:
            if r.status == 200:
                return await r.json(content_type=None)
            log.debug(f"[13F] {url} → {r.status}")
            return None
    except Exception as e:
        log.debug(f"[13F] request failed {url}: {e}")
        return None


async def x__get__mutmut_8(session: aiohttp.ClientSession, url: str) -> dict | list | None:
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=16)) as r:
            if r.status == 200:
                return await r.json(content_type=None)
            log.debug(f"[13F] {url} → {r.status}")
            return None
    except Exception as e:
        log.debug(f"[13F] request failed {url}: {e}")
        return None


async def x__get__mutmut_9(session: aiohttp.ClientSession, url: str) -> dict | list | None:
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
            if r.status != 200:
                return await r.json(content_type=None)
            log.debug(f"[13F] {url} → {r.status}")
            return None
    except Exception as e:
        log.debug(f"[13F] request failed {url}: {e}")
        return None


async def x__get__mutmut_10(session: aiohttp.ClientSession, url: str) -> dict | list | None:
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
            if r.status == 201:
                return await r.json(content_type=None)
            log.debug(f"[13F] {url} → {r.status}")
            return None
    except Exception as e:
        log.debug(f"[13F] request failed {url}: {e}")
        return None


async def x__get__mutmut_11(session: aiohttp.ClientSession, url: str) -> dict | list | None:
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
            if r.status == 200:
                return await r.json(content_type=None)
            log.debug(None)
            return None
    except Exception as e:
        log.debug(f"[13F] request failed {url}: {e}")
        return None


async def x__get__mutmut_12(session: aiohttp.ClientSession, url: str) -> dict | list | None:
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
            if r.status == 200:
                return await r.json(content_type=None)
            log.debug(f"[13F] {url} → {r.status}")
            return None
    except Exception as e:
        log.debug(None)
        return None

mutants_x__get__mutmut['_mutmut_orig'] = x__get__mutmut_orig # type: ignore # mutmut generated
mutants_x__get__mutmut['x__get__mutmut_1'] = x__get__mutmut_1 # type: ignore # mutmut generated
mutants_x__get__mutmut['x__get__mutmut_2'] = x__get__mutmut_2 # type: ignore # mutmut generated
mutants_x__get__mutmut['x__get__mutmut_3'] = x__get__mutmut_3 # type: ignore # mutmut generated
mutants_x__get__mutmut['x__get__mutmut_4'] = x__get__mutmut_4 # type: ignore # mutmut generated
mutants_x__get__mutmut['x__get__mutmut_5'] = x__get__mutmut_5 # type: ignore # mutmut generated
mutants_x__get__mutmut['x__get__mutmut_6'] = x__get__mutmut_6 # type: ignore # mutmut generated
mutants_x__get__mutmut['x__get__mutmut_7'] = x__get__mutmut_7 # type: ignore # mutmut generated
mutants_x__get__mutmut['x__get__mutmut_8'] = x__get__mutmut_8 # type: ignore # mutmut generated
mutants_x__get__mutmut['x__get__mutmut_9'] = x__get__mutmut_9 # type: ignore # mutmut generated
mutants_x__get__mutmut['x__get__mutmut_10'] = x__get__mutmut_10 # type: ignore # mutmut generated
mutants_x__get__mutmut['x__get__mutmut_11'] = x__get__mutmut_11 # type: ignore # mutmut generated
mutants_x__get__mutmut['x__get__mutmut_12'] = x__get__mutmut_12 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__fetch_latest_13f_holdings__mutmut)
async def _fetch_latest_13f_holdings(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_orig(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_1(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = None
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_2(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key not in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_3(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() + _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_4(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["XXtsXX"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_5(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["TS"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_6(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] <= _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_7(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["XXdataXX"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_8(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["DATA"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_9(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = None
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_10(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = None
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_11(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(None, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_12(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, None)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_13(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_14(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, )
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_15(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_16(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = None
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_17(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get(None, {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_18(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", None)
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_19(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get({})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_20(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", )
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_21(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get(None, {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_22(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", None).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_23(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get({}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_24(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", ).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_25(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("XXfilingsXX", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_26(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("FILINGS", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_27(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("XXrecentXX", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_28(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("RECENT", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_29(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = None
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_30(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get(None, [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_31(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", None)
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_32(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get([])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_33(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", )
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_34(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("XXformXX", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_35(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("FORM", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_36(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = None
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_37(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get(None, [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_38(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", None)
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_39(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get([])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_40(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", )
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_41(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("XXfilingDateXX", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_42(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingdate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_43(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("FILINGDATE", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_44(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = None

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_45(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get(None, [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_46(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", None)

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_47(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get([])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_48(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", )

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_49(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("XXaccessionNumberXX", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_50(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionnumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_51(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("ACCESSIONNUMBER", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_52(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = None

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_53(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(None) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_54(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form not in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_55(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("XX13F-HRXX", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_56(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13f-hr", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_57(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "XX13F-HR/AXX")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_58(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13f-hr/a")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_59(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:4]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_60(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_61(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = None
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_62(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = None
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_63(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace(None, "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_64(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", None)
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_65(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_66(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", )
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_67(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("XX-XX", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_68(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "XXXX")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_69(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = None
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_70(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(None)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_71(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = None
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_72(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(None, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_73(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, None)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_74(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_75(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, )
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_76(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_77(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            break

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_78(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = ""
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_79(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) and []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_80(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get(None, []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_81(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", None) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_82(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get([]) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_83(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", ) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_84(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get(None, {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_85(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", None).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_86(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get({}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_87(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", ).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_88(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("XXdirectoryXX", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_89(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("DIRECTORY", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_90(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("XXitemXX", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_91(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("ITEM", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_92(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = None
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_93(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get(None, "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_94(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", None)
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_95(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_96(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", )
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_97(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("XXnameXX", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_98(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("NAME", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_99(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "XXXX")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_100(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() or name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_101(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "XXinfotableXX" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_102(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "INFOTABLE" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_103(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" not in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_104(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.upper() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_105(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(None):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_106(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith("XX.xmlXX"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_107(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".XML"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_108(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = None
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_109(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(None)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_110(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                return

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_111(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_112(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            break

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_113(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = None
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_114(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(None, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_115(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, None, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_116(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, None)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_117(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_118(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_119(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, )
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_120(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(None)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_121(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(None)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_122(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(1.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_123(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_124(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = None
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_125(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["XXcusipXX"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_126(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["CUSIP"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_127(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[1]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_128(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = None
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_129(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["XXcusipXX"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_130(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["CUSIP"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_131(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_132(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) >= 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_133(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 2 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_134(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = None

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_135(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["XXcusipXX"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_136(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["CUSIP"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_137(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[3]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_138(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) >= 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_139(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 3 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_140(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = None
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_141(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_142(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get(None):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_143(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("XXtickerXX"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_144(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("TICKER"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_145(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            break

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_146(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip not in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_147(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = None
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_148(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["XXsharesXX"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_149(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["SHARES"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_150(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["XXsharesXX"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_151(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["SHARES"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_152(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] >= prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_153(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares / 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_154(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 2.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_155(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = None
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_156(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "XXincreasedXX"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_157(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "INCREASED"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_158(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["XXsharesXX"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_159(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["SHARES"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_160(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] <= prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_161(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares / 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_162(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 1.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_163(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = None
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_164(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "XXdecreasedXX"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_165(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "DECREASED"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_166(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = None
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_167(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "XXunchangedXX"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_168(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "UNCHANGED"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_169(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = None

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_170(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "XXnewXX"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_171(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "NEW"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_172(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = None
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_173(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "XXneutralXX"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_174(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "NEUTRAL"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_175(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous or cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_176(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") or cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_177(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action not in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_178(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("XXincreasedXX", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_179(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("INCREASED", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_180(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "XXnewXX") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_181(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "NEW") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_182(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip not in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_183(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip not in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_184(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = None
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_185(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["XXsharesXX"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_186(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["SHARES"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_187(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = None
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_188(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["XXsharesXX"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_189(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["SHARES"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_190(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares >= q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_191(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares / 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_192(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 2.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_193(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = None
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_194(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "XXrisingXX"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_195(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "RISING"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_196(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares <= q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_197(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares / 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_198(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 1.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_199(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = None
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_200(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "XXreversing_upXX"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_201(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "REVERSING_UP"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_202(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous or cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_203(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" or cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_204(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action != "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_205(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "XXdecreasedXX" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_206(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "DECREASED" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_207(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip not in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_208(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip not in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_209(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = None
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_210(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["XXsharesXX"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_211(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["SHARES"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_212(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = None
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_213(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["XXsharesXX"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_214(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["SHARES"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_215(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares <= q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_216(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares / 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_217(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 1.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_218(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = None
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_219(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "XXfallingXX"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_220(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "FALLING"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_221(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares >= q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_222(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares / 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_223(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 2.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_224(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = None

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_225(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "XXreversing_downXX"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_226(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "REVERSING_DOWN"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_227(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append(None)

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_228(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "XXactionXX": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_229(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "ACTION": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_230(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "XXqoq_trendXX": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_231(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "QOQ_TREND": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_232(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = None
    return result


async def x__fetch_latest_13f_holdings__mutmut_233(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"XXtsXX": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_234(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"TS": time.time(), "data": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_235(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "XXdataXX": result}
    return result


async def x__fetch_latest_13f_holdings__mutmut_236(session: aiohttp.ClientSession, cik: str) -> list[dict]:
    """
    Fetch the most recent 13F-HR filing holdings for a given CIK.
    Returns a list of {"ticker": str, "value_k": int, "shares": int, "action": str}
    where action is "new" | "increased" | "decreased" | "unchanged".
    """
    cache_key = f"13f_{cik}"
    if cache_key in _CACHE:
        if time.time() - _CACHE[cache_key]["ts"] < _CACHE_TTL:
            return _CACHE[cache_key]["data"]

    # Get filing list
    filings_url = f"https://data.sec.gov/submissions/CIK{cik}.json"
    data = await _get(session, filings_url)
    if not data:
        return []

    recent = data.get("filings", {}).get("recent", {})
    forms = recent.get("form", [])
    dates = recent.get("filingDate", [])
    accnos = recent.get("accessionNumber", [])

    # Find the three most recent 13F-HR filings (Q0, Q1, Q2) for QoQ trend tracking
    filings_13f = [(dates[i], accnos[i]) for i, form in enumerate(forms) if form in ("13F-HR", "13F-HR/A")][:3]

    if not filings_13f:
        return []

    holdings_by_quarter = []
    for filing_date, accno in filings_13f:
        accno_fmt = accno.replace("-", "")
        index_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{accno}-index.json"
        index_data = await _get(session, index_url)
        if not index_data:
            continue

        # Find the primary document (infotable.xml or similar)
        xml_url = None
        for item in index_data.get("directory", {}).get("item", []) or []:
            name = item.get("name", "")
            if "infotable" in name.lower() and name.endswith(".xml"):
                xml_url = f"https://data.sec.gov/Archives/edgar/data/{int(cik)}/{accno_fmt}/{name}"
                break

        if not xml_url:
            continue

        holdings = await _parse_infotable_xml(session, xml_url, filing_date)
        holdings_by_quarter.append(holdings)
        await asyncio.sleep(0.15)  # SEC rate limit: 10 req/sec

    if not holdings_by_quarter:
        return []

    current = {h["cusip"]: h for h in holdings_by_quarter[0]}
    previous = {h["cusip"]: h for h in holdings_by_quarter[1]} if len(holdings_by_quarter) > 1 else {}
    two_ago = {h["cusip"]: h for h in holdings_by_quarter[2]} if len(holdings_by_quarter) > 2 else {}

    result = []
    for cusip, h in current.items():
        if not h.get("ticker"):
            continue

        # Q0 vs Q1 action
        if cusip in previous:
            prev_shares = previous[cusip]["shares"]
            if h["shares"] > prev_shares * 1.05:
                action = "increased"
            elif h["shares"] < prev_shares * 0.95:
                action = "decreased"
            else:
                action = "unchanged"
        else:
            action = "new"

        # QoQ trend: compare Q1 direction vs Q2 direction to detect consecutive momentum
        qoq_trend = "neutral"
        if action in ("increased", "new") and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares > q2_shares * 1.05:
                # Two consecutive quarters of increases → rising conviction
                qoq_trend = "rising"
            elif q1_shares < q2_shares * 0.95:
                # Increased this quarter but decreased last quarter → reversing
                qoq_trend = "reversing_up"
        elif action == "decreased" and cusip in previous and cusip in two_ago:
            q1_shares = previous[cusip]["shares"]
            q2_shares = two_ago[cusip]["shares"]
            if q1_shares < q2_shares * 0.95:
                # Two consecutive quarters of decreases → falling conviction / early exit
                qoq_trend = "falling"
            elif q1_shares > q2_shares * 1.05:
                qoq_trend = "reversing_down"

        result.append({**h, "action": action, "qoq_trend": qoq_trend})

    _CACHE[cache_key] = {"ts": time.time(), "DATA": result}
    return result

mutants_x__fetch_latest_13f_holdings__mutmut['_mutmut_orig'] = x__fetch_latest_13f_holdings__mutmut_orig # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_1'] = x__fetch_latest_13f_holdings__mutmut_1 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_2'] = x__fetch_latest_13f_holdings__mutmut_2 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_3'] = x__fetch_latest_13f_holdings__mutmut_3 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_4'] = x__fetch_latest_13f_holdings__mutmut_4 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_5'] = x__fetch_latest_13f_holdings__mutmut_5 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_6'] = x__fetch_latest_13f_holdings__mutmut_6 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_7'] = x__fetch_latest_13f_holdings__mutmut_7 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_8'] = x__fetch_latest_13f_holdings__mutmut_8 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_9'] = x__fetch_latest_13f_holdings__mutmut_9 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_10'] = x__fetch_latest_13f_holdings__mutmut_10 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_11'] = x__fetch_latest_13f_holdings__mutmut_11 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_12'] = x__fetch_latest_13f_holdings__mutmut_12 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_13'] = x__fetch_latest_13f_holdings__mutmut_13 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_14'] = x__fetch_latest_13f_holdings__mutmut_14 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_15'] = x__fetch_latest_13f_holdings__mutmut_15 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_16'] = x__fetch_latest_13f_holdings__mutmut_16 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_17'] = x__fetch_latest_13f_holdings__mutmut_17 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_18'] = x__fetch_latest_13f_holdings__mutmut_18 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_19'] = x__fetch_latest_13f_holdings__mutmut_19 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_20'] = x__fetch_latest_13f_holdings__mutmut_20 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_21'] = x__fetch_latest_13f_holdings__mutmut_21 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_22'] = x__fetch_latest_13f_holdings__mutmut_22 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_23'] = x__fetch_latest_13f_holdings__mutmut_23 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_24'] = x__fetch_latest_13f_holdings__mutmut_24 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_25'] = x__fetch_latest_13f_holdings__mutmut_25 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_26'] = x__fetch_latest_13f_holdings__mutmut_26 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_27'] = x__fetch_latest_13f_holdings__mutmut_27 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_28'] = x__fetch_latest_13f_holdings__mutmut_28 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_29'] = x__fetch_latest_13f_holdings__mutmut_29 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_30'] = x__fetch_latest_13f_holdings__mutmut_30 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_31'] = x__fetch_latest_13f_holdings__mutmut_31 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_32'] = x__fetch_latest_13f_holdings__mutmut_32 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_33'] = x__fetch_latest_13f_holdings__mutmut_33 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_34'] = x__fetch_latest_13f_holdings__mutmut_34 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_35'] = x__fetch_latest_13f_holdings__mutmut_35 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_36'] = x__fetch_latest_13f_holdings__mutmut_36 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_37'] = x__fetch_latest_13f_holdings__mutmut_37 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_38'] = x__fetch_latest_13f_holdings__mutmut_38 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_39'] = x__fetch_latest_13f_holdings__mutmut_39 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_40'] = x__fetch_latest_13f_holdings__mutmut_40 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_41'] = x__fetch_latest_13f_holdings__mutmut_41 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_42'] = x__fetch_latest_13f_holdings__mutmut_42 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_43'] = x__fetch_latest_13f_holdings__mutmut_43 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_44'] = x__fetch_latest_13f_holdings__mutmut_44 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_45'] = x__fetch_latest_13f_holdings__mutmut_45 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_46'] = x__fetch_latest_13f_holdings__mutmut_46 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_47'] = x__fetch_latest_13f_holdings__mutmut_47 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_48'] = x__fetch_latest_13f_holdings__mutmut_48 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_49'] = x__fetch_latest_13f_holdings__mutmut_49 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_50'] = x__fetch_latest_13f_holdings__mutmut_50 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_51'] = x__fetch_latest_13f_holdings__mutmut_51 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_52'] = x__fetch_latest_13f_holdings__mutmut_52 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_53'] = x__fetch_latest_13f_holdings__mutmut_53 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_54'] = x__fetch_latest_13f_holdings__mutmut_54 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_55'] = x__fetch_latest_13f_holdings__mutmut_55 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_56'] = x__fetch_latest_13f_holdings__mutmut_56 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_57'] = x__fetch_latest_13f_holdings__mutmut_57 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_58'] = x__fetch_latest_13f_holdings__mutmut_58 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_59'] = x__fetch_latest_13f_holdings__mutmut_59 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_60'] = x__fetch_latest_13f_holdings__mutmut_60 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_61'] = x__fetch_latest_13f_holdings__mutmut_61 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_62'] = x__fetch_latest_13f_holdings__mutmut_62 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_63'] = x__fetch_latest_13f_holdings__mutmut_63 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_64'] = x__fetch_latest_13f_holdings__mutmut_64 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_65'] = x__fetch_latest_13f_holdings__mutmut_65 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_66'] = x__fetch_latest_13f_holdings__mutmut_66 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_67'] = x__fetch_latest_13f_holdings__mutmut_67 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_68'] = x__fetch_latest_13f_holdings__mutmut_68 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_69'] = x__fetch_latest_13f_holdings__mutmut_69 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_70'] = x__fetch_latest_13f_holdings__mutmut_70 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_71'] = x__fetch_latest_13f_holdings__mutmut_71 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_72'] = x__fetch_latest_13f_holdings__mutmut_72 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_73'] = x__fetch_latest_13f_holdings__mutmut_73 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_74'] = x__fetch_latest_13f_holdings__mutmut_74 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_75'] = x__fetch_latest_13f_holdings__mutmut_75 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_76'] = x__fetch_latest_13f_holdings__mutmut_76 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_77'] = x__fetch_latest_13f_holdings__mutmut_77 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_78'] = x__fetch_latest_13f_holdings__mutmut_78 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_79'] = x__fetch_latest_13f_holdings__mutmut_79 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_80'] = x__fetch_latest_13f_holdings__mutmut_80 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_81'] = x__fetch_latest_13f_holdings__mutmut_81 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_82'] = x__fetch_latest_13f_holdings__mutmut_82 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_83'] = x__fetch_latest_13f_holdings__mutmut_83 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_84'] = x__fetch_latest_13f_holdings__mutmut_84 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_85'] = x__fetch_latest_13f_holdings__mutmut_85 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_86'] = x__fetch_latest_13f_holdings__mutmut_86 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_87'] = x__fetch_latest_13f_holdings__mutmut_87 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_88'] = x__fetch_latest_13f_holdings__mutmut_88 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_89'] = x__fetch_latest_13f_holdings__mutmut_89 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_90'] = x__fetch_latest_13f_holdings__mutmut_90 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_91'] = x__fetch_latest_13f_holdings__mutmut_91 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_92'] = x__fetch_latest_13f_holdings__mutmut_92 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_93'] = x__fetch_latest_13f_holdings__mutmut_93 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_94'] = x__fetch_latest_13f_holdings__mutmut_94 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_95'] = x__fetch_latest_13f_holdings__mutmut_95 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_96'] = x__fetch_latest_13f_holdings__mutmut_96 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_97'] = x__fetch_latest_13f_holdings__mutmut_97 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_98'] = x__fetch_latest_13f_holdings__mutmut_98 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_99'] = x__fetch_latest_13f_holdings__mutmut_99 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_100'] = x__fetch_latest_13f_holdings__mutmut_100 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_101'] = x__fetch_latest_13f_holdings__mutmut_101 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_102'] = x__fetch_latest_13f_holdings__mutmut_102 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_103'] = x__fetch_latest_13f_holdings__mutmut_103 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_104'] = x__fetch_latest_13f_holdings__mutmut_104 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_105'] = x__fetch_latest_13f_holdings__mutmut_105 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_106'] = x__fetch_latest_13f_holdings__mutmut_106 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_107'] = x__fetch_latest_13f_holdings__mutmut_107 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_108'] = x__fetch_latest_13f_holdings__mutmut_108 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_109'] = x__fetch_latest_13f_holdings__mutmut_109 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_110'] = x__fetch_latest_13f_holdings__mutmut_110 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_111'] = x__fetch_latest_13f_holdings__mutmut_111 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_112'] = x__fetch_latest_13f_holdings__mutmut_112 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_113'] = x__fetch_latest_13f_holdings__mutmut_113 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_114'] = x__fetch_latest_13f_holdings__mutmut_114 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_115'] = x__fetch_latest_13f_holdings__mutmut_115 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_116'] = x__fetch_latest_13f_holdings__mutmut_116 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_117'] = x__fetch_latest_13f_holdings__mutmut_117 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_118'] = x__fetch_latest_13f_holdings__mutmut_118 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_119'] = x__fetch_latest_13f_holdings__mutmut_119 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_120'] = x__fetch_latest_13f_holdings__mutmut_120 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_121'] = x__fetch_latest_13f_holdings__mutmut_121 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_122'] = x__fetch_latest_13f_holdings__mutmut_122 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_123'] = x__fetch_latest_13f_holdings__mutmut_123 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_124'] = x__fetch_latest_13f_holdings__mutmut_124 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_125'] = x__fetch_latest_13f_holdings__mutmut_125 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_126'] = x__fetch_latest_13f_holdings__mutmut_126 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_127'] = x__fetch_latest_13f_holdings__mutmut_127 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_128'] = x__fetch_latest_13f_holdings__mutmut_128 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_129'] = x__fetch_latest_13f_holdings__mutmut_129 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_130'] = x__fetch_latest_13f_holdings__mutmut_130 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_131'] = x__fetch_latest_13f_holdings__mutmut_131 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_132'] = x__fetch_latest_13f_holdings__mutmut_132 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_133'] = x__fetch_latest_13f_holdings__mutmut_133 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_134'] = x__fetch_latest_13f_holdings__mutmut_134 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_135'] = x__fetch_latest_13f_holdings__mutmut_135 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_136'] = x__fetch_latest_13f_holdings__mutmut_136 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_137'] = x__fetch_latest_13f_holdings__mutmut_137 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_138'] = x__fetch_latest_13f_holdings__mutmut_138 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_139'] = x__fetch_latest_13f_holdings__mutmut_139 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_140'] = x__fetch_latest_13f_holdings__mutmut_140 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_141'] = x__fetch_latest_13f_holdings__mutmut_141 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_142'] = x__fetch_latest_13f_holdings__mutmut_142 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_143'] = x__fetch_latest_13f_holdings__mutmut_143 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_144'] = x__fetch_latest_13f_holdings__mutmut_144 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_145'] = x__fetch_latest_13f_holdings__mutmut_145 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_146'] = x__fetch_latest_13f_holdings__mutmut_146 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_147'] = x__fetch_latest_13f_holdings__mutmut_147 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_148'] = x__fetch_latest_13f_holdings__mutmut_148 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_149'] = x__fetch_latest_13f_holdings__mutmut_149 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_150'] = x__fetch_latest_13f_holdings__mutmut_150 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_151'] = x__fetch_latest_13f_holdings__mutmut_151 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_152'] = x__fetch_latest_13f_holdings__mutmut_152 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_153'] = x__fetch_latest_13f_holdings__mutmut_153 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_154'] = x__fetch_latest_13f_holdings__mutmut_154 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_155'] = x__fetch_latest_13f_holdings__mutmut_155 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_156'] = x__fetch_latest_13f_holdings__mutmut_156 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_157'] = x__fetch_latest_13f_holdings__mutmut_157 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_158'] = x__fetch_latest_13f_holdings__mutmut_158 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_159'] = x__fetch_latest_13f_holdings__mutmut_159 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_160'] = x__fetch_latest_13f_holdings__mutmut_160 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_161'] = x__fetch_latest_13f_holdings__mutmut_161 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_162'] = x__fetch_latest_13f_holdings__mutmut_162 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_163'] = x__fetch_latest_13f_holdings__mutmut_163 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_164'] = x__fetch_latest_13f_holdings__mutmut_164 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_165'] = x__fetch_latest_13f_holdings__mutmut_165 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_166'] = x__fetch_latest_13f_holdings__mutmut_166 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_167'] = x__fetch_latest_13f_holdings__mutmut_167 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_168'] = x__fetch_latest_13f_holdings__mutmut_168 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_169'] = x__fetch_latest_13f_holdings__mutmut_169 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_170'] = x__fetch_latest_13f_holdings__mutmut_170 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_171'] = x__fetch_latest_13f_holdings__mutmut_171 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_172'] = x__fetch_latest_13f_holdings__mutmut_172 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_173'] = x__fetch_latest_13f_holdings__mutmut_173 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_174'] = x__fetch_latest_13f_holdings__mutmut_174 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_175'] = x__fetch_latest_13f_holdings__mutmut_175 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_176'] = x__fetch_latest_13f_holdings__mutmut_176 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_177'] = x__fetch_latest_13f_holdings__mutmut_177 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_178'] = x__fetch_latest_13f_holdings__mutmut_178 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_179'] = x__fetch_latest_13f_holdings__mutmut_179 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_180'] = x__fetch_latest_13f_holdings__mutmut_180 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_181'] = x__fetch_latest_13f_holdings__mutmut_181 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_182'] = x__fetch_latest_13f_holdings__mutmut_182 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_183'] = x__fetch_latest_13f_holdings__mutmut_183 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_184'] = x__fetch_latest_13f_holdings__mutmut_184 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_185'] = x__fetch_latest_13f_holdings__mutmut_185 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_186'] = x__fetch_latest_13f_holdings__mutmut_186 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_187'] = x__fetch_latest_13f_holdings__mutmut_187 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_188'] = x__fetch_latest_13f_holdings__mutmut_188 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_189'] = x__fetch_latest_13f_holdings__mutmut_189 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_190'] = x__fetch_latest_13f_holdings__mutmut_190 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_191'] = x__fetch_latest_13f_holdings__mutmut_191 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_192'] = x__fetch_latest_13f_holdings__mutmut_192 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_193'] = x__fetch_latest_13f_holdings__mutmut_193 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_194'] = x__fetch_latest_13f_holdings__mutmut_194 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_195'] = x__fetch_latest_13f_holdings__mutmut_195 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_196'] = x__fetch_latest_13f_holdings__mutmut_196 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_197'] = x__fetch_latest_13f_holdings__mutmut_197 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_198'] = x__fetch_latest_13f_holdings__mutmut_198 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_199'] = x__fetch_latest_13f_holdings__mutmut_199 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_200'] = x__fetch_latest_13f_holdings__mutmut_200 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_201'] = x__fetch_latest_13f_holdings__mutmut_201 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_202'] = x__fetch_latest_13f_holdings__mutmut_202 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_203'] = x__fetch_latest_13f_holdings__mutmut_203 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_204'] = x__fetch_latest_13f_holdings__mutmut_204 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_205'] = x__fetch_latest_13f_holdings__mutmut_205 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_206'] = x__fetch_latest_13f_holdings__mutmut_206 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_207'] = x__fetch_latest_13f_holdings__mutmut_207 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_208'] = x__fetch_latest_13f_holdings__mutmut_208 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_209'] = x__fetch_latest_13f_holdings__mutmut_209 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_210'] = x__fetch_latest_13f_holdings__mutmut_210 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_211'] = x__fetch_latest_13f_holdings__mutmut_211 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_212'] = x__fetch_latest_13f_holdings__mutmut_212 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_213'] = x__fetch_latest_13f_holdings__mutmut_213 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_214'] = x__fetch_latest_13f_holdings__mutmut_214 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_215'] = x__fetch_latest_13f_holdings__mutmut_215 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_216'] = x__fetch_latest_13f_holdings__mutmut_216 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_217'] = x__fetch_latest_13f_holdings__mutmut_217 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_218'] = x__fetch_latest_13f_holdings__mutmut_218 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_219'] = x__fetch_latest_13f_holdings__mutmut_219 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_220'] = x__fetch_latest_13f_holdings__mutmut_220 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_221'] = x__fetch_latest_13f_holdings__mutmut_221 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_222'] = x__fetch_latest_13f_holdings__mutmut_222 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_223'] = x__fetch_latest_13f_holdings__mutmut_223 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_224'] = x__fetch_latest_13f_holdings__mutmut_224 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_225'] = x__fetch_latest_13f_holdings__mutmut_225 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_226'] = x__fetch_latest_13f_holdings__mutmut_226 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_227'] = x__fetch_latest_13f_holdings__mutmut_227 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_228'] = x__fetch_latest_13f_holdings__mutmut_228 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_229'] = x__fetch_latest_13f_holdings__mutmut_229 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_230'] = x__fetch_latest_13f_holdings__mutmut_230 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_231'] = x__fetch_latest_13f_holdings__mutmut_231 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_232'] = x__fetch_latest_13f_holdings__mutmut_232 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_233'] = x__fetch_latest_13f_holdings__mutmut_233 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_234'] = x__fetch_latest_13f_holdings__mutmut_234 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_235'] = x__fetch_latest_13f_holdings__mutmut_235 # type: ignore # mutmut generated
mutants_x__fetch_latest_13f_holdings__mutmut['x__fetch_latest_13f_holdings__mutmut_236'] = x__fetch_latest_13f_holdings__mutmut_236 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__parse_infotable_xml__mutmut)
async def _parse_infotable_xml(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_orig(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_1(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(None, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_2(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=None, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_3(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=None) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_4(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_5(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_6(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, ) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_7(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=None)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_8(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=21)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_9(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status == 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_10(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 201:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_11(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = None
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_12(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = None
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_13(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = None
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_14(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(None, ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_15(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', None)
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_16(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_17(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', )
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_18(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace('XX xmlns="XX', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_19(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' XMLNS="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_20(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', 'XX xmlns_orig="XX')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_21(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' XMLNS_ORIG="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_22(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = None

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_23(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(None)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_24(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter(None):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_25(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("XXinfoTableXX"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_26(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infotable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_27(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("INFOTABLE"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_28(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = None
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_29(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") and "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_30(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext(None) or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_31(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("XXnameOfIssuerXX") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_32(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameofissuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_33(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("NAMEOFISSUER") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_34(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "XXXX").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_35(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = None
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_36(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") and "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_37(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext(None) or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_38(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("XXcusipXX") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_39(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("CUSIP") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_40(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "XXXX").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_41(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = None  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_42(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(None)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_43(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") and 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_44(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext(None) or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_45(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("XXvalueXX") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_46(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("VALUE") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_47(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 1)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_48(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = None
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_49(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(None)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_50(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") and 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_51(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext(None) or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_52(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("XXsshPrnamtXX") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_53(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshprnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_54(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("SSHPRNAMT") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_55(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 1)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_56(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = None
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_57(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(None)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_58(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_59(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                break
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_60(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                None
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_61(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "XXnameXX": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_62(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "NAME": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_63(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "XXtickerXX": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_64(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "TICKER": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_65(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "XXcusipXX": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_66(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "CUSIP": cusip,
                    "value_k": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_67(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "XXvalue_kXX": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_68(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "VALUE_K": value,
                    "shares": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_69(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "XXsharesXX": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_70(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "SHARES": shares,
                    "filing_date": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_71(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "XXfiling_dateXX": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings


async def x__parse_infotable_xml__mutmut_72(session: aiohttp.ClientSession, url: str, filing_date: str) -> list[dict]:
    """Parse an SEC 13F infotable XML into a list of holdings."""
    try:
        async with session.get(url, headers=_SEC_HEADERS, timeout=aiohttp.ClientTimeout(total=20)) as r:
            if r.status != 200:
                return []
            text = await r.text()
    except Exception:
        return []

    import xml.etree.ElementTree as ET

    holdings = []
    try:
        # Strip namespace for simpler parsing
        text_clean = text.replace(' xmlns="', ' xmlns_orig="')
        root = ET.fromstring(text_clean)

        for entry in root.iter("infoTable"):
            name = (entry.findtext("nameOfIssuer") or "").strip()
            cusip = (entry.findtext("cusip") or "").strip()
            value = int(entry.findtext("value") or 0)  # thousands USD
            shares = int(entry.findtext("sshPrnamt") or 0)
            ticker = _cusip_to_ticker(cusip)
            if not ticker:
                continue
            holdings.append(
                {
                    "name": name,
                    "ticker": ticker,
                    "cusip": cusip,
                    "value_k": value,
                    "shares": shares,
                    "FILING_DATE": filing_date,
                }
            )
    except ET.ParseError:
        pass

    return holdings

mutants_x__parse_infotable_xml__mutmut['_mutmut_orig'] = x__parse_infotable_xml__mutmut_orig # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_1'] = x__parse_infotable_xml__mutmut_1 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_2'] = x__parse_infotable_xml__mutmut_2 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_3'] = x__parse_infotable_xml__mutmut_3 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_4'] = x__parse_infotable_xml__mutmut_4 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_5'] = x__parse_infotable_xml__mutmut_5 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_6'] = x__parse_infotable_xml__mutmut_6 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_7'] = x__parse_infotable_xml__mutmut_7 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_8'] = x__parse_infotable_xml__mutmut_8 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_9'] = x__parse_infotable_xml__mutmut_9 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_10'] = x__parse_infotable_xml__mutmut_10 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_11'] = x__parse_infotable_xml__mutmut_11 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_12'] = x__parse_infotable_xml__mutmut_12 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_13'] = x__parse_infotable_xml__mutmut_13 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_14'] = x__parse_infotable_xml__mutmut_14 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_15'] = x__parse_infotable_xml__mutmut_15 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_16'] = x__parse_infotable_xml__mutmut_16 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_17'] = x__parse_infotable_xml__mutmut_17 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_18'] = x__parse_infotable_xml__mutmut_18 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_19'] = x__parse_infotable_xml__mutmut_19 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_20'] = x__parse_infotable_xml__mutmut_20 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_21'] = x__parse_infotable_xml__mutmut_21 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_22'] = x__parse_infotable_xml__mutmut_22 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_23'] = x__parse_infotable_xml__mutmut_23 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_24'] = x__parse_infotable_xml__mutmut_24 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_25'] = x__parse_infotable_xml__mutmut_25 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_26'] = x__parse_infotable_xml__mutmut_26 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_27'] = x__parse_infotable_xml__mutmut_27 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_28'] = x__parse_infotable_xml__mutmut_28 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_29'] = x__parse_infotable_xml__mutmut_29 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_30'] = x__parse_infotable_xml__mutmut_30 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_31'] = x__parse_infotable_xml__mutmut_31 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_32'] = x__parse_infotable_xml__mutmut_32 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_33'] = x__parse_infotable_xml__mutmut_33 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_34'] = x__parse_infotable_xml__mutmut_34 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_35'] = x__parse_infotable_xml__mutmut_35 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_36'] = x__parse_infotable_xml__mutmut_36 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_37'] = x__parse_infotable_xml__mutmut_37 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_38'] = x__parse_infotable_xml__mutmut_38 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_39'] = x__parse_infotable_xml__mutmut_39 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_40'] = x__parse_infotable_xml__mutmut_40 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_41'] = x__parse_infotable_xml__mutmut_41 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_42'] = x__parse_infotable_xml__mutmut_42 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_43'] = x__parse_infotable_xml__mutmut_43 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_44'] = x__parse_infotable_xml__mutmut_44 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_45'] = x__parse_infotable_xml__mutmut_45 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_46'] = x__parse_infotable_xml__mutmut_46 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_47'] = x__parse_infotable_xml__mutmut_47 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_48'] = x__parse_infotable_xml__mutmut_48 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_49'] = x__parse_infotable_xml__mutmut_49 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_50'] = x__parse_infotable_xml__mutmut_50 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_51'] = x__parse_infotable_xml__mutmut_51 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_52'] = x__parse_infotable_xml__mutmut_52 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_53'] = x__parse_infotable_xml__mutmut_53 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_54'] = x__parse_infotable_xml__mutmut_54 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_55'] = x__parse_infotable_xml__mutmut_55 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_56'] = x__parse_infotable_xml__mutmut_56 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_57'] = x__parse_infotable_xml__mutmut_57 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_58'] = x__parse_infotable_xml__mutmut_58 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_59'] = x__parse_infotable_xml__mutmut_59 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_60'] = x__parse_infotable_xml__mutmut_60 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_61'] = x__parse_infotable_xml__mutmut_61 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_62'] = x__parse_infotable_xml__mutmut_62 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_63'] = x__parse_infotable_xml__mutmut_63 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_64'] = x__parse_infotable_xml__mutmut_64 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_65'] = x__parse_infotable_xml__mutmut_65 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_66'] = x__parse_infotable_xml__mutmut_66 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_67'] = x__parse_infotable_xml__mutmut_67 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_68'] = x__parse_infotable_xml__mutmut_68 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_69'] = x__parse_infotable_xml__mutmut_69 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_70'] = x__parse_infotable_xml__mutmut_70 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_71'] = x__parse_infotable_xml__mutmut_71 # type: ignore # mutmut generated
mutants_x__parse_infotable_xml__mutmut['x__parse_infotable_xml__mutmut_72'] = x__parse_infotable_xml__mutmut_72 # type: ignore # mutmut generated


# CUSIP → ticker mapping (common large-caps that appear in 13F filings)
# Deduplicated — each CUSIP appears only once
_CUSIP_MAP: dict[str, str] = {
    "037833100": "AAPL",
    "594918104": "MSFT",
    "023135106": "AMZN",
    "02079K305": "GOOGL",
    "30303M102": "META",
    "88160R101": "TSLA",
    "67066G104": "NVDA",
    "922908363": "V",
    "57636Q104": "MA",
    "46625H100": "JPM",
    "172967424": "CRM",
    "69608A108": "ORCL",
    "742718109": "PG",
    "478160104": "JNJ",
    "931427108": "UNH",
    "911312106": "UPS",
    "732834107": "PFE",
    "857477103": "STZ",
    "079879106": "BRK-B",
    "91822M106": "V",
    "345370860": "AMD",
    "78486M107": "S&P",
    "12504L109": "COP",
    "458140100": "INTC",
    "883556102": "TM",
    "66987V109": "NVO",
    "025816109": "AXP",
}
mutants_x__cusip_to_ticker__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__cusip_to_ticker__mutmut)
def _cusip_to_ticker(cusip: str) -> str | None:
    """Look up ticker for a CUSIP. Falls back to None for unknown CUSIPs."""
    return _CUSIP_MAP.get(cusip.strip())


def x__cusip_to_ticker__mutmut_orig(cusip: str) -> str | None:
    """Look up ticker for a CUSIP. Falls back to None for unknown CUSIPs."""
    return _CUSIP_MAP.get(cusip.strip())


def x__cusip_to_ticker__mutmut_1(cusip: str) -> str | None:
    """Look up ticker for a CUSIP. Falls back to None for unknown CUSIPs."""
    return _CUSIP_MAP.get(None)

mutants_x__cusip_to_ticker__mutmut['_mutmut_orig'] = x__cusip_to_ticker__mutmut_orig # type: ignore # mutmut generated
mutants_x__cusip_to_ticker__mutmut['x__cusip_to_ticker__mutmut_1'] = x__cusip_to_ticker__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_institutional_signals__mutmut)
async def get_institutional_signals(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_orig(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_1(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = None
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_2(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(None)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_3(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.lower() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_4(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = None

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_5(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = None
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_6(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(None, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_7(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, None)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_8(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_9(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, )
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_10(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["XXtickerXX"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_11(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["TICKER"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_12(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_13(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        break

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_14(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = None
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_15(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(None, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_16(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, None)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_17(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_18(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, )
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_19(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 4)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_20(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = None

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_21(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get(None, "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_22(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", None)

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_23(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_24(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", )

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_25(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("XXqoq_trendXX", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_26(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("QOQ_TREND", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_27(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "XXneutralXX")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_28(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "NEUTRAL")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_29(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = None
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_30(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "XXrisingXX": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_31(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "RISING": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_32(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 2.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_33(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "XXreversing_upXX": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_34(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "REVERSING_UP": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_35(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 2.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_36(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "XXneutralXX": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_37(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "NEUTRAL": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_38(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 2.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_39(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "XXreversing_downXX": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_40(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "REVERSING_DOWN": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_41(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 2.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_42(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "XXfallingXX": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_43(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "FALLING": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_44(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 2.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_45(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = None
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_46(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(None, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_47(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, None)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_48(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_49(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, )
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_50(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 2.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_51(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = None

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_52(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(None, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_53(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, None)

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_54(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get("")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_55(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, )

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_56(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "XXrisingXX": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_57(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "RISING": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_58(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": "XX [QoQ Rising — 2+ quarters buying]XX",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_59(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [qoq rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_60(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QOQ RISING — 2+ QUARTERS BUYING]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_61(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "XXreversing_upXX": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_62(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "REVERSING_UP": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_63(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": "XX [QoQ Reversing Up — new buying trend]XX",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_64(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [qoq reversing up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_65(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QOQ REVERSING UP — NEW BUYING TREND]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_66(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "XXneutralXX": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_67(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "NEUTRAL": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_68(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "XXXX",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_69(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "XXreversing_downXX": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_70(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "REVERSING_DOWN": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_71(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "XXXX",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_72(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "XXfallingXX": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_73(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "FALLING": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_74(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": "XX [QoQ Falling — 2+ quarters exiting]XX",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_75(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [qoq falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_76(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QOQ FALLING — 2+ QUARTERS EXITING]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_77(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "XXXX")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_78(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["XXactionXX"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_79(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["ACTION"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_80(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] not in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_81(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("XXnewXX", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_82(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("NEW", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_83(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "XXincreasedXX"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_84(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "INCREASED"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_85(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["XXactionXX"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_86(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["ACTION"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_87(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] != "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_88(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "XXnewXX":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_89(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "NEW":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_90(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = None
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_91(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round(None)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_92(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) / qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_93(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score - 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_94(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 4) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_95(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = None
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_96(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "XXopened new positionXX"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_97(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "OPENED NEW POSITION"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_98(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = None
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_99(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(None)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_100(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score / qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_101(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = None

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_102(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "XXincreased positionXX"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_103(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "INCREASED POSITION"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_104(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            None
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_105(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "XXtickerXX": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_106(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "TICKER": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_107(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["XXtickerXX"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_108(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["TICKER"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_109(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "XXscoreXX": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_110(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "SCORE": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_111(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "XXactionXX": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_112(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "ACTION": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_113(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "XXBUYXX",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_114(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "buy",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_115(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "XXrationaleXX": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_116(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "RATIONALE": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_117(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['XXfiling_dateXX']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_118(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['FILING_DATE']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_119(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['XXvalue_kXX']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_120(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['VALUE_K']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_121(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['XXsharesXX']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_122(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['SHARES']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_123(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "XXsourceXX": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_124(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "SOURCE": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_125(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "XX13FXX",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_126(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13f",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_127(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "XXfundXX": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_128(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "FUND": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_129(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "XXtierXX": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_130(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "TIER": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_131(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "XXfiling_dateXX": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_132(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "FILING_DATE": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_133(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["XXfiling_dateXX"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_134(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["FILING_DATE"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_135(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "XXqoq_trendXX": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_136(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "QOQ_TREND": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_137(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["XXactionXX"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_138(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["ACTION"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_139(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] not in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_140(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("XXdecreasedXX",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_141(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("DECREASED",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_142(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = None
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_143(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(None)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_144(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score / qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_145(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(+base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_146(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            None
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_147(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "XXtickerXX": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_148(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "TICKER": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_149(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["XXtickerXX"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_150(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["TICKER"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_151(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "XXscoreXX": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_152(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "SCORE": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_153(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "XXactionXX": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_154(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "ACTION": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_155(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "XXSELLXX",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_156(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "sell",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_157(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "XXrationaleXX": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_158(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "RATIONALE": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_159(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['XXfiling_dateXX']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_160(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['FILING_DATE']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_161(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['XXvalue_kXX']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_162(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['VALUE_K']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_163(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['XXsharesXX']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_164(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['SHARES']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_165(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "XXsourceXX": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_166(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "SOURCE": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_167(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "XX13FXX",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_168(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13f",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_169(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "XXfundXX": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_170(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "FUND": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_171(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "XXtierXX": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_172(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "TIER": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_173(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "XXfiling_dateXX": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_174(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "FILING_DATE": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_175(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["XXfiling_dateXX"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_176(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["FILING_DATE"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_177(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "XXqoq_trendXX": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_178(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "QOQ_TREND": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_179(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(None)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_180(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(1.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_181(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(None)
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_182(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                break

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_183(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = None
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_184(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = None
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_185(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["XXtickerXX"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_186(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["TICKER"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_187(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_188(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = None
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_189(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "XXtickerXX": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_190(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "TICKER": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_191(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "XXscoreXX": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_192(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "SCORE": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_193(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 1,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_194(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "XXrationale_itemsXX": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_195(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "RATIONALE_ITEMS": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_196(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "XXfundsXX": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_197(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "FUNDS": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_198(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "XXactionsXX": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_199(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "ACTIONS": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_200(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "XXqoq_trendsXX": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_201(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "QOQ_TRENDS": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_202(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] = r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_203(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] -= r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_204(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["XXscoreXX"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_205(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["SCORE"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_206(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["XXscoreXX"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_207(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["SCORE"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_208(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(None)
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_209(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["XXfundsXX"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_210(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["FUNDS"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_211(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["XXfundXX"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_212(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["FUND"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_213(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(None)
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_214(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["XXrationale_itemsXX"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_215(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["RATIONALE_ITEMS"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_216(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["XXrationaleXX"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_217(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["RATIONALE"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_218(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(None)
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_219(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["XXactionsXX"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_220(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["ACTIONS"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_221(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["XXactionXX"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_222(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["ACTION"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_223(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(None)

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_224(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["XXqoq_trendsXX"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_225(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["QOQ_TRENDS"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_226(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get(None, "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_227(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", None))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_228(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_229(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", ))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_230(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("XXqoq_trendXX", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_231(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("QOQ_TREND", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_232(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "XXneutralXX"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_233(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "NEUTRAL"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_234(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = None
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_235(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = None
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_236(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(None, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_237(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, None)
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_238(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_239(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, )
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_240(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(+20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_241(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-21, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_242(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(None, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_243(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, None))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_244(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_245(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, ))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_246(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(21, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_247(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["XXscoreXX"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_248(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["SCORE"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_249(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = None
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_250(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(None)
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_251(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = "XX, XX".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_252(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["XXfundsXX"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_253(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["FUNDS"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_254(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:4])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_255(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = None

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_256(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) + 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_257(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 4} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_258(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) >= 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_259(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 4 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_260(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else "XXXX"

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_261(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = None
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_262(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "XXposXX" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_263(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "POS" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_264(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score >= 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_265(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 1 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_266(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "XXnegXX" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_267(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "NEG" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_268(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score <= 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_269(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 1 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_270(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "XXneuXX"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_271(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "NEU"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_272(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = None

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_273(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "XXbuyingXX" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_274(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "BUYING" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_275(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score >= 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_276(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 1 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_277(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "XXsellingXX" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_278(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "SELLING" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_279(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score <= 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_280(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 1 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_281(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "XXmixedXX"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_282(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "MIXED"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_283(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = None
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_284(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(None) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_285(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["XXqoq_trendsXX"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_286(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["QOQ_TRENDS"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_287(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(None)}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_288(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["XXqoq_trendsXX"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_289(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["QOQ_TRENDS"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_290(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = None
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_291(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(None, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_292(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=None) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_293(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_294(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, ) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_295(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "XXneutralXX"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_296(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "NEUTRAL"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_297(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = None

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_298(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(None, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_299(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, None)

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_300(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get("")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_301(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, )

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_302(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "XXrisingXX": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_303(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "RISING": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_304(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": "XX · Rising QoQ ConvictionXX",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_305(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · rising qoq conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_306(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · RISING QOQ CONVICTION",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_307(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "XXfallingXX": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_308(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "FALLING": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_309(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": "XX · Falling QoQ — Early Exit SignalXX",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_310(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · falling qoq — early exit signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_311(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · FALLING QOQ — EARLY EXIT SIGNAL",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_312(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "XXreversing_upXX": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_313(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "REVERSING_UP": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_314(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": "XX · QoQ Turning BullishXX",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_315(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · qoq turning bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_316(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QOQ TURNING BULLISH",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_317(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "XXreversing_downXX": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_318(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "REVERSING_DOWN": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_319(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "XXXX",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_320(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "XXneutralXX": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_321(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "NEUTRAL": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_322(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "XXXX",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_323(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "XXXX")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_324(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            None
        )

    return aggregated


async def x_get_institutional_signals__mutmut_325(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "XXtickerXX": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_326(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "TICKER": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_327(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "XXscoreXX": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_328(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "SCORE": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_329(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "XXsourceXX": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_330(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "SOURCE": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_331(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "XX13FXX",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_332(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13f",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_333(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "XXqoq_trendXX": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_334(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "QOQ_TREND": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_335(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "XXrationaleXX": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_336(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "RATIONALE": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_337(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "XXsrcXX": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_338(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "SRC": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_339(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "XX13FXX",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_340(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13f",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_341(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "XXheadXX": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_342(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "HEAD": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_343(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "XXdetailXX": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_344(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "DETAIL": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_345(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["XXrationale_itemsXX"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_346(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["RATIONALE_ITEMS"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_347(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][1] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_348(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["XXrationale_itemsXX"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_349(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["RATIONALE_ITEMS"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_350(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "XXXX",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_351(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "XXsentimentXX": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_352(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "SENTIMENT": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_353(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "XXbodyXX": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_354(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "BODY": data["rationale_items"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_355(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["XXrationale_itemsXX"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_356(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["RATIONALE_ITEMS"][0] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_357(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][1] if data["rationale_items"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_358(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["XXrationale_itemsXX"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_359(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["RATIONALE_ITEMS"] else "",
                },
            }
        )

    return aggregated


async def x_get_institutional_signals__mutmut_360(watchlist: list[str]) -> list[dict]:
    """
    Main entry point: fetch 13F signals for tickers in the watchlist.
    Returns list of signal dicts with keys: ticker, score, rationale, sources.
    Tracks both institutional buying AND selling for balanced signals.
    """
    watchlist_set = set(t.upper() for t in watchlist)
    results: list[dict] = []

    async with shared_session() as session:
        for fund_name, cik, tier in TRACKED_FUNDS:
            try:
                holdings = await _fetch_latest_13f_holdings(session, cik)
                for h in holdings:
                    if h["ticker"] not in watchlist_set:
                        continue

                    base_score = TIER_SCORES.get(tier, 3)
                    qoq_trend = h.get("qoq_trend", "neutral")

                    # QoQ multiplier: consecutive direction = stronger signal
                    QOQ_MULT = {
                        "rising": 1.5,  # 2+ quarters increasing → high conviction
                        "reversing_up": 1.1,  # started buying after selling
                        "neutral": 1.0,
                        "reversing_down": 1.0,  # started selling after buying — caution
                        "falling": 1.5,  # 2+ quarters decreasing → early exit warning
                    }
                    qoq_mult = QOQ_MULT.get(qoq_trend, 1.0)
                    qoq_note = {
                        "rising": " [QoQ Rising — 2+ quarters buying]",
                        "reversing_up": " [QoQ Reversing Up — new buying trend]",
                        "neutral": "",
                        "reversing_down": "",
                        "falling": " [QoQ Falling — 2+ quarters exiting]",
                    }.get(qoq_trend, "")

                    # Track both buys and sells
                    if h["action"] in ("new", "increased"):
                        if h["action"] == "new":
                            score = round((base_score + 3) * qoq_mult)
                            action_label = "opened new position"
                        else:
                            score = round(base_score * qoq_mult)
                            action_label = "increased position"

                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "BUY",
                                "rationale": f"{fund_name} {action_label} as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                    elif h["action"] in ("decreased",):
                        # Falling QoQ trend amplifies the bearish score
                        score = round(-base_score * qoq_mult)
                        results.append(
                            {
                                "ticker": h["ticker"],
                                "score": score,
                                "action": "SELL",
                                "rationale": f"{fund_name} decreased position as of {h['filing_date']} (${h['value_k']:,}K, {h['shares']:,} shares){qoq_note}",
                                "source": "13F",
                                "fund": fund_name,
                                "tier": tier,
                                "filing_date": h["filing_date"],
                                "qoq_trend": qoq_trend,
                            }
                        )
                await asyncio.sleep(0.1)  # SEC rate limit
            except Exception as e:
                log.debug(f"[13F] {fund_name}: {e}")
                continue

    # Aggregate by ticker — multiple funds holding = stronger signal
    by_ticker: dict = {}
    for r in results:
        t = r["ticker"]
        if t not in by_ticker:
            by_ticker[t] = {
                "ticker": t,
                "score": 0,
                "rationale_items": [],
                "funds": [],
                "actions": [],
                "qoq_trends": [],
            }
        by_ticker[t]["score"] += r["score"]
        by_ticker[t]["funds"].append(r["fund"])
        by_ticker[t]["rationale_items"].append(r["rationale"])
        by_ticker[t]["actions"].append(r["action"])
        by_ticker[t]["qoq_trends"].append(r.get("qoq_trend", "neutral"))

    aggregated = []
    for t, data in by_ticker.items():
        # Cap score at ±20 to avoid drowning out other signals
        capped_score = max(-20, min(20, data["score"]))
        fund_list = ", ".join(data["funds"][:3])
        extra = f" +{len(data['funds']) - 3} more" if len(data["funds"]) > 3 else ""

        # Determine overall sentiment and QoQ trend label
        sentiment = "pos" if capped_score > 0 else "neg" if capped_score < 0 else "neu"
        action_label = "buying" if capped_score > 0 else "selling" if capped_score < 0 else "mixed"

        # Surface the strongest QoQ trend signal for the rationale head
        trend_counts = {tr: data["qoq_trends"].count(tr) for tr in set(data["qoq_trends"])}
        dominant_trend = max(trend_counts, key=trend_counts.get) if trend_counts else "neutral"
        trend_suffix = {
            "rising": " · Rising QoQ Conviction",
            "falling": " · Falling QoQ — Early Exit Signal",
            "reversing_up": " · QoQ Turning Bullish",
            "reversing_down": "",
            "neutral": "",
        }.get(dominant_trend, "")

        aggregated.append(
            {
                "ticker": t,
                "score": capped_score,
                "source": "13F",
                "qoq_trend": dominant_trend,
                "rationale": {
                    "src": "13F",
                    "head": f"Institutional {action_label}: {fund_list}{extra}{trend_suffix}",
                    "detail": data["rationale_items"][0] if data["rationale_items"] else "",
                    "sentiment": sentiment,
                    "body": data["rationale_items"][0] if data["rationale_items"] else "XXXX",
                },
            }
        )

    return aggregated

mutants_x_get_institutional_signals__mutmut['_mutmut_orig'] = x_get_institutional_signals__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_1'] = x_get_institutional_signals__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_2'] = x_get_institutional_signals__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_3'] = x_get_institutional_signals__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_4'] = x_get_institutional_signals__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_5'] = x_get_institutional_signals__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_6'] = x_get_institutional_signals__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_7'] = x_get_institutional_signals__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_8'] = x_get_institutional_signals__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_9'] = x_get_institutional_signals__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_10'] = x_get_institutional_signals__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_11'] = x_get_institutional_signals__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_12'] = x_get_institutional_signals__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_13'] = x_get_institutional_signals__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_14'] = x_get_institutional_signals__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_15'] = x_get_institutional_signals__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_16'] = x_get_institutional_signals__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_17'] = x_get_institutional_signals__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_18'] = x_get_institutional_signals__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_19'] = x_get_institutional_signals__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_20'] = x_get_institutional_signals__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_21'] = x_get_institutional_signals__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_22'] = x_get_institutional_signals__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_23'] = x_get_institutional_signals__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_24'] = x_get_institutional_signals__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_25'] = x_get_institutional_signals__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_26'] = x_get_institutional_signals__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_27'] = x_get_institutional_signals__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_28'] = x_get_institutional_signals__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_29'] = x_get_institutional_signals__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_30'] = x_get_institutional_signals__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_31'] = x_get_institutional_signals__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_32'] = x_get_institutional_signals__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_33'] = x_get_institutional_signals__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_34'] = x_get_institutional_signals__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_35'] = x_get_institutional_signals__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_36'] = x_get_institutional_signals__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_37'] = x_get_institutional_signals__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_38'] = x_get_institutional_signals__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_39'] = x_get_institutional_signals__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_40'] = x_get_institutional_signals__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_41'] = x_get_institutional_signals__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_42'] = x_get_institutional_signals__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_43'] = x_get_institutional_signals__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_44'] = x_get_institutional_signals__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_45'] = x_get_institutional_signals__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_46'] = x_get_institutional_signals__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_47'] = x_get_institutional_signals__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_48'] = x_get_institutional_signals__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_49'] = x_get_institutional_signals__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_50'] = x_get_institutional_signals__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_51'] = x_get_institutional_signals__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_52'] = x_get_institutional_signals__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_53'] = x_get_institutional_signals__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_54'] = x_get_institutional_signals__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_55'] = x_get_institutional_signals__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_56'] = x_get_institutional_signals__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_57'] = x_get_institutional_signals__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_58'] = x_get_institutional_signals__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_59'] = x_get_institutional_signals__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_60'] = x_get_institutional_signals__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_61'] = x_get_institutional_signals__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_62'] = x_get_institutional_signals__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_63'] = x_get_institutional_signals__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_64'] = x_get_institutional_signals__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_65'] = x_get_institutional_signals__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_66'] = x_get_institutional_signals__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_67'] = x_get_institutional_signals__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_68'] = x_get_institutional_signals__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_69'] = x_get_institutional_signals__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_70'] = x_get_institutional_signals__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_71'] = x_get_institutional_signals__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_72'] = x_get_institutional_signals__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_73'] = x_get_institutional_signals__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_74'] = x_get_institutional_signals__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_75'] = x_get_institutional_signals__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_76'] = x_get_institutional_signals__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_77'] = x_get_institutional_signals__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_78'] = x_get_institutional_signals__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_79'] = x_get_institutional_signals__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_80'] = x_get_institutional_signals__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_81'] = x_get_institutional_signals__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_82'] = x_get_institutional_signals__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_83'] = x_get_institutional_signals__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_84'] = x_get_institutional_signals__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_85'] = x_get_institutional_signals__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_86'] = x_get_institutional_signals__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_87'] = x_get_institutional_signals__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_88'] = x_get_institutional_signals__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_89'] = x_get_institutional_signals__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_90'] = x_get_institutional_signals__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_91'] = x_get_institutional_signals__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_92'] = x_get_institutional_signals__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_93'] = x_get_institutional_signals__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_94'] = x_get_institutional_signals__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_95'] = x_get_institutional_signals__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_96'] = x_get_institutional_signals__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_97'] = x_get_institutional_signals__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_98'] = x_get_institutional_signals__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_99'] = x_get_institutional_signals__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_100'] = x_get_institutional_signals__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_101'] = x_get_institutional_signals__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_102'] = x_get_institutional_signals__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_103'] = x_get_institutional_signals__mutmut_103 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_104'] = x_get_institutional_signals__mutmut_104 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_105'] = x_get_institutional_signals__mutmut_105 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_106'] = x_get_institutional_signals__mutmut_106 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_107'] = x_get_institutional_signals__mutmut_107 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_108'] = x_get_institutional_signals__mutmut_108 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_109'] = x_get_institutional_signals__mutmut_109 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_110'] = x_get_institutional_signals__mutmut_110 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_111'] = x_get_institutional_signals__mutmut_111 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_112'] = x_get_institutional_signals__mutmut_112 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_113'] = x_get_institutional_signals__mutmut_113 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_114'] = x_get_institutional_signals__mutmut_114 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_115'] = x_get_institutional_signals__mutmut_115 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_116'] = x_get_institutional_signals__mutmut_116 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_117'] = x_get_institutional_signals__mutmut_117 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_118'] = x_get_institutional_signals__mutmut_118 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_119'] = x_get_institutional_signals__mutmut_119 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_120'] = x_get_institutional_signals__mutmut_120 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_121'] = x_get_institutional_signals__mutmut_121 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_122'] = x_get_institutional_signals__mutmut_122 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_123'] = x_get_institutional_signals__mutmut_123 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_124'] = x_get_institutional_signals__mutmut_124 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_125'] = x_get_institutional_signals__mutmut_125 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_126'] = x_get_institutional_signals__mutmut_126 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_127'] = x_get_institutional_signals__mutmut_127 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_128'] = x_get_institutional_signals__mutmut_128 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_129'] = x_get_institutional_signals__mutmut_129 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_130'] = x_get_institutional_signals__mutmut_130 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_131'] = x_get_institutional_signals__mutmut_131 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_132'] = x_get_institutional_signals__mutmut_132 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_133'] = x_get_institutional_signals__mutmut_133 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_134'] = x_get_institutional_signals__mutmut_134 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_135'] = x_get_institutional_signals__mutmut_135 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_136'] = x_get_institutional_signals__mutmut_136 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_137'] = x_get_institutional_signals__mutmut_137 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_138'] = x_get_institutional_signals__mutmut_138 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_139'] = x_get_institutional_signals__mutmut_139 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_140'] = x_get_institutional_signals__mutmut_140 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_141'] = x_get_institutional_signals__mutmut_141 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_142'] = x_get_institutional_signals__mutmut_142 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_143'] = x_get_institutional_signals__mutmut_143 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_144'] = x_get_institutional_signals__mutmut_144 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_145'] = x_get_institutional_signals__mutmut_145 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_146'] = x_get_institutional_signals__mutmut_146 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_147'] = x_get_institutional_signals__mutmut_147 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_148'] = x_get_institutional_signals__mutmut_148 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_149'] = x_get_institutional_signals__mutmut_149 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_150'] = x_get_institutional_signals__mutmut_150 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_151'] = x_get_institutional_signals__mutmut_151 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_152'] = x_get_institutional_signals__mutmut_152 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_153'] = x_get_institutional_signals__mutmut_153 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_154'] = x_get_institutional_signals__mutmut_154 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_155'] = x_get_institutional_signals__mutmut_155 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_156'] = x_get_institutional_signals__mutmut_156 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_157'] = x_get_institutional_signals__mutmut_157 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_158'] = x_get_institutional_signals__mutmut_158 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_159'] = x_get_institutional_signals__mutmut_159 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_160'] = x_get_institutional_signals__mutmut_160 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_161'] = x_get_institutional_signals__mutmut_161 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_162'] = x_get_institutional_signals__mutmut_162 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_163'] = x_get_institutional_signals__mutmut_163 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_164'] = x_get_institutional_signals__mutmut_164 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_165'] = x_get_institutional_signals__mutmut_165 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_166'] = x_get_institutional_signals__mutmut_166 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_167'] = x_get_institutional_signals__mutmut_167 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_168'] = x_get_institutional_signals__mutmut_168 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_169'] = x_get_institutional_signals__mutmut_169 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_170'] = x_get_institutional_signals__mutmut_170 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_171'] = x_get_institutional_signals__mutmut_171 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_172'] = x_get_institutional_signals__mutmut_172 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_173'] = x_get_institutional_signals__mutmut_173 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_174'] = x_get_institutional_signals__mutmut_174 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_175'] = x_get_institutional_signals__mutmut_175 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_176'] = x_get_institutional_signals__mutmut_176 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_177'] = x_get_institutional_signals__mutmut_177 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_178'] = x_get_institutional_signals__mutmut_178 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_179'] = x_get_institutional_signals__mutmut_179 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_180'] = x_get_institutional_signals__mutmut_180 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_181'] = x_get_institutional_signals__mutmut_181 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_182'] = x_get_institutional_signals__mutmut_182 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_183'] = x_get_institutional_signals__mutmut_183 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_184'] = x_get_institutional_signals__mutmut_184 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_185'] = x_get_institutional_signals__mutmut_185 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_186'] = x_get_institutional_signals__mutmut_186 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_187'] = x_get_institutional_signals__mutmut_187 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_188'] = x_get_institutional_signals__mutmut_188 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_189'] = x_get_institutional_signals__mutmut_189 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_190'] = x_get_institutional_signals__mutmut_190 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_191'] = x_get_institutional_signals__mutmut_191 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_192'] = x_get_institutional_signals__mutmut_192 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_193'] = x_get_institutional_signals__mutmut_193 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_194'] = x_get_institutional_signals__mutmut_194 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_195'] = x_get_institutional_signals__mutmut_195 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_196'] = x_get_institutional_signals__mutmut_196 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_197'] = x_get_institutional_signals__mutmut_197 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_198'] = x_get_institutional_signals__mutmut_198 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_199'] = x_get_institutional_signals__mutmut_199 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_200'] = x_get_institutional_signals__mutmut_200 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_201'] = x_get_institutional_signals__mutmut_201 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_202'] = x_get_institutional_signals__mutmut_202 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_203'] = x_get_institutional_signals__mutmut_203 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_204'] = x_get_institutional_signals__mutmut_204 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_205'] = x_get_institutional_signals__mutmut_205 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_206'] = x_get_institutional_signals__mutmut_206 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_207'] = x_get_institutional_signals__mutmut_207 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_208'] = x_get_institutional_signals__mutmut_208 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_209'] = x_get_institutional_signals__mutmut_209 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_210'] = x_get_institutional_signals__mutmut_210 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_211'] = x_get_institutional_signals__mutmut_211 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_212'] = x_get_institutional_signals__mutmut_212 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_213'] = x_get_institutional_signals__mutmut_213 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_214'] = x_get_institutional_signals__mutmut_214 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_215'] = x_get_institutional_signals__mutmut_215 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_216'] = x_get_institutional_signals__mutmut_216 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_217'] = x_get_institutional_signals__mutmut_217 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_218'] = x_get_institutional_signals__mutmut_218 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_219'] = x_get_institutional_signals__mutmut_219 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_220'] = x_get_institutional_signals__mutmut_220 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_221'] = x_get_institutional_signals__mutmut_221 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_222'] = x_get_institutional_signals__mutmut_222 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_223'] = x_get_institutional_signals__mutmut_223 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_224'] = x_get_institutional_signals__mutmut_224 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_225'] = x_get_institutional_signals__mutmut_225 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_226'] = x_get_institutional_signals__mutmut_226 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_227'] = x_get_institutional_signals__mutmut_227 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_228'] = x_get_institutional_signals__mutmut_228 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_229'] = x_get_institutional_signals__mutmut_229 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_230'] = x_get_institutional_signals__mutmut_230 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_231'] = x_get_institutional_signals__mutmut_231 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_232'] = x_get_institutional_signals__mutmut_232 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_233'] = x_get_institutional_signals__mutmut_233 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_234'] = x_get_institutional_signals__mutmut_234 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_235'] = x_get_institutional_signals__mutmut_235 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_236'] = x_get_institutional_signals__mutmut_236 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_237'] = x_get_institutional_signals__mutmut_237 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_238'] = x_get_institutional_signals__mutmut_238 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_239'] = x_get_institutional_signals__mutmut_239 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_240'] = x_get_institutional_signals__mutmut_240 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_241'] = x_get_institutional_signals__mutmut_241 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_242'] = x_get_institutional_signals__mutmut_242 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_243'] = x_get_institutional_signals__mutmut_243 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_244'] = x_get_institutional_signals__mutmut_244 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_245'] = x_get_institutional_signals__mutmut_245 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_246'] = x_get_institutional_signals__mutmut_246 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_247'] = x_get_institutional_signals__mutmut_247 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_248'] = x_get_institutional_signals__mutmut_248 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_249'] = x_get_institutional_signals__mutmut_249 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_250'] = x_get_institutional_signals__mutmut_250 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_251'] = x_get_institutional_signals__mutmut_251 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_252'] = x_get_institutional_signals__mutmut_252 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_253'] = x_get_institutional_signals__mutmut_253 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_254'] = x_get_institutional_signals__mutmut_254 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_255'] = x_get_institutional_signals__mutmut_255 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_256'] = x_get_institutional_signals__mutmut_256 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_257'] = x_get_institutional_signals__mutmut_257 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_258'] = x_get_institutional_signals__mutmut_258 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_259'] = x_get_institutional_signals__mutmut_259 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_260'] = x_get_institutional_signals__mutmut_260 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_261'] = x_get_institutional_signals__mutmut_261 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_262'] = x_get_institutional_signals__mutmut_262 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_263'] = x_get_institutional_signals__mutmut_263 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_264'] = x_get_institutional_signals__mutmut_264 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_265'] = x_get_institutional_signals__mutmut_265 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_266'] = x_get_institutional_signals__mutmut_266 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_267'] = x_get_institutional_signals__mutmut_267 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_268'] = x_get_institutional_signals__mutmut_268 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_269'] = x_get_institutional_signals__mutmut_269 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_270'] = x_get_institutional_signals__mutmut_270 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_271'] = x_get_institutional_signals__mutmut_271 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_272'] = x_get_institutional_signals__mutmut_272 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_273'] = x_get_institutional_signals__mutmut_273 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_274'] = x_get_institutional_signals__mutmut_274 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_275'] = x_get_institutional_signals__mutmut_275 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_276'] = x_get_institutional_signals__mutmut_276 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_277'] = x_get_institutional_signals__mutmut_277 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_278'] = x_get_institutional_signals__mutmut_278 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_279'] = x_get_institutional_signals__mutmut_279 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_280'] = x_get_institutional_signals__mutmut_280 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_281'] = x_get_institutional_signals__mutmut_281 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_282'] = x_get_institutional_signals__mutmut_282 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_283'] = x_get_institutional_signals__mutmut_283 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_284'] = x_get_institutional_signals__mutmut_284 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_285'] = x_get_institutional_signals__mutmut_285 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_286'] = x_get_institutional_signals__mutmut_286 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_287'] = x_get_institutional_signals__mutmut_287 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_288'] = x_get_institutional_signals__mutmut_288 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_289'] = x_get_institutional_signals__mutmut_289 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_290'] = x_get_institutional_signals__mutmut_290 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_291'] = x_get_institutional_signals__mutmut_291 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_292'] = x_get_institutional_signals__mutmut_292 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_293'] = x_get_institutional_signals__mutmut_293 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_294'] = x_get_institutional_signals__mutmut_294 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_295'] = x_get_institutional_signals__mutmut_295 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_296'] = x_get_institutional_signals__mutmut_296 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_297'] = x_get_institutional_signals__mutmut_297 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_298'] = x_get_institutional_signals__mutmut_298 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_299'] = x_get_institutional_signals__mutmut_299 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_300'] = x_get_institutional_signals__mutmut_300 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_301'] = x_get_institutional_signals__mutmut_301 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_302'] = x_get_institutional_signals__mutmut_302 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_303'] = x_get_institutional_signals__mutmut_303 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_304'] = x_get_institutional_signals__mutmut_304 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_305'] = x_get_institutional_signals__mutmut_305 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_306'] = x_get_institutional_signals__mutmut_306 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_307'] = x_get_institutional_signals__mutmut_307 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_308'] = x_get_institutional_signals__mutmut_308 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_309'] = x_get_institutional_signals__mutmut_309 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_310'] = x_get_institutional_signals__mutmut_310 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_311'] = x_get_institutional_signals__mutmut_311 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_312'] = x_get_institutional_signals__mutmut_312 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_313'] = x_get_institutional_signals__mutmut_313 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_314'] = x_get_institutional_signals__mutmut_314 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_315'] = x_get_institutional_signals__mutmut_315 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_316'] = x_get_institutional_signals__mutmut_316 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_317'] = x_get_institutional_signals__mutmut_317 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_318'] = x_get_institutional_signals__mutmut_318 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_319'] = x_get_institutional_signals__mutmut_319 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_320'] = x_get_institutional_signals__mutmut_320 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_321'] = x_get_institutional_signals__mutmut_321 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_322'] = x_get_institutional_signals__mutmut_322 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_323'] = x_get_institutional_signals__mutmut_323 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_324'] = x_get_institutional_signals__mutmut_324 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_325'] = x_get_institutional_signals__mutmut_325 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_326'] = x_get_institutional_signals__mutmut_326 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_327'] = x_get_institutional_signals__mutmut_327 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_328'] = x_get_institutional_signals__mutmut_328 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_329'] = x_get_institutional_signals__mutmut_329 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_330'] = x_get_institutional_signals__mutmut_330 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_331'] = x_get_institutional_signals__mutmut_331 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_332'] = x_get_institutional_signals__mutmut_332 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_333'] = x_get_institutional_signals__mutmut_333 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_334'] = x_get_institutional_signals__mutmut_334 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_335'] = x_get_institutional_signals__mutmut_335 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_336'] = x_get_institutional_signals__mutmut_336 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_337'] = x_get_institutional_signals__mutmut_337 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_338'] = x_get_institutional_signals__mutmut_338 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_339'] = x_get_institutional_signals__mutmut_339 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_340'] = x_get_institutional_signals__mutmut_340 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_341'] = x_get_institutional_signals__mutmut_341 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_342'] = x_get_institutional_signals__mutmut_342 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_343'] = x_get_institutional_signals__mutmut_343 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_344'] = x_get_institutional_signals__mutmut_344 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_345'] = x_get_institutional_signals__mutmut_345 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_346'] = x_get_institutional_signals__mutmut_346 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_347'] = x_get_institutional_signals__mutmut_347 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_348'] = x_get_institutional_signals__mutmut_348 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_349'] = x_get_institutional_signals__mutmut_349 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_350'] = x_get_institutional_signals__mutmut_350 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_351'] = x_get_institutional_signals__mutmut_351 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_352'] = x_get_institutional_signals__mutmut_352 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_353'] = x_get_institutional_signals__mutmut_353 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_354'] = x_get_institutional_signals__mutmut_354 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_355'] = x_get_institutional_signals__mutmut_355 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_356'] = x_get_institutional_signals__mutmut_356 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_357'] = x_get_institutional_signals__mutmut_357 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_358'] = x_get_institutional_signals__mutmut_358 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_359'] = x_get_institutional_signals__mutmut_359 # type: ignore # mutmut generated
mutants_x_get_institutional_signals__mutmut['x_get_institutional_signals__mutmut_360'] = x_get_institutional_signals__mutmut_360 # type: ignore # mutmut generated
